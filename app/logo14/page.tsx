// TEMPORARY logo bake-off route — delete with the rest of the experiment.
import LogoPage from "@/app/components/logo-experiment/LogoPage";

export const dynamic = "force-dynamic";

export default function Page() {
  return <LogoPage slug="logo14" />;
}
