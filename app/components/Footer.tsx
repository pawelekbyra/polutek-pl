'use client';

import React from 'react';
import Link from 'next/link';

const Footer = () => {
  return (
    <footer className="py-6 px-8 bg-zinc-900 border-t border-zinc-800 flex justify-center items-center">
      <span className="font-brand font-black text-[12px] uppercase tracking-[0.3em] text-zinc-500 opacity-60">
        WWW.POLUTEK.PL
      </span>
    </footer>
  );
};

export default Footer;
