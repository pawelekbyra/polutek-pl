# CLAUDE.md — AI Agent Guide for Polutek.pl

This file is the primary entry point for AI agents working on this codebase. Read it in full before touching any code.

The full product documentation index lives at `docs/README.md` (architecture, specs, runbooks, audits, ticket queue). Historical multi-agent process docs were retired 2026-07-02 — do not recreate reconciliation reports, roadmaps, or role protocols; durable conclusions belong here, in `KNOWN_LIMITATIONS.md`, or in `docs/audit/`.

**Maintenance rule:** When you add a meaningful new feature, change a critical invariant, introduce a new module, or rename a key file, update this document. Future agents depend on it being accurate.

---

## 0. Current State for Agents

Runtime foundations are stabilized around the current invariants in §4: `PatronGrant` is the source of truth, legacy `User.isPatron` cache fields no longer exist, payment success must route through `fulfillPayment()`, and denied playback plans must not expose playable URLs, tokens, provider IDs, or mounted players.

Do not infer launch readiness from green CI or documentation alone. Public launch still depends on owner/legal/operator evidence, production smoke proof, and the final launch decision tracked outside the executable coding queue, especially GitHub issue #1269.

The executable coding queue is `docs/tickets/ready/`. If a GitHub issue or doc says work is still open but the code/schema/docs already prove it is done, update or close that issue instead of re-implementing old work.

**Rebrand as of 2026-07-30: POLUTEK.PL → PAWELPERFECT.PL.** The owner-facing brand name, footer mark, legal-document "Administratorem serwisu ... jest" owner line, PWA manifest `name`/`short_name`, page-title/metadata fallbacks, the `APP_NAME`/`APP_DOMAIN` constants in `lib/constants.ts`, and the transactional-email `from` fallback in `legacy-email-service-provider.ts` were all updated to PAWELPERFECT.PL. Deliberately left untouched, and why:
- `MAIN_CREATOR_SLUG`'s `'polutek'` fallback (`lib/constants.ts`, `prisma/seed.ts`) — this is a database lookup key (a real `Creator.slug` row), not display text; renaming the code fallback without renaming the actual DB row would break the public home page if the real `MAIN_CREATOR_SLUG` env var were ever unset. Only touch this together with an actual DB migration.
- The Clerk/CSP hostnames in `lib/utils/security.ts` (`clerk.polutek.pl`, `accounts.polutek.pl`, `polutek.pl`, `*.polutek.pl`) — these are real DNS/Clerk custom-domain entries. Removing them from the CSP allow-list before Clerk's custom domain is actually migrated to the new domain would break production login immediately. **Update 2026-07-31:** `generateCSP()`'s `clerkDomains` list is no longer purely static — `getConfiguredAppHosts()` derives host entries from `NEXT_PUBLIC_APP_URL` (adding `clerk.<host>`, `accounts.<host>`, `<host>`, `*.<host>`) and always includes both `polutek.pl` and `pawelperfect.pl` as static fallbacks, so a Clerk custom-domain cutover to pawelperfect.pl can't get silently CSP-blocked by this file being stale again. Keep both domains present until the `polutek.pl` alias is fully retired in Vercel/Clerk; do not narrow this back down to a single domain without confirming the old alias is gone. New admin diagnostic endpoint: `GET /api/admin/health/clerk` (admin-only, mirrors the `admin/health/cloudflare` pattern) decodes `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`'s embedded Frontend API host, flags a mismatch against `NEXT_PUBLIC_APP_URL`'s domain, checks whether that host is present in the live CSP, and (with `?probe=network`) does a live reachability check against it — use this first when Clerk UI silently fails to render after a domain change, since a stale publishable/secret key pair encoding the old domain is the most common cause and won't show up as a normal error.
- Purely internal identifiers with no user-facing surface — CSS/JS hooks like `.polutek-vidstack-player`, `.polutek-watch-nav`, `.polutek-player-loader*`, the `PolutekControls.tsx` component name, and custom event names like `polutek:open-support` — left alone; renaming them is a large mechanical refactor across many files for zero user-visible benefit.
- The GitHub repository name/owner and actual DNS — code changes here don't move the live site. `polutek.pl`/`www.polutek.pl` were still the production Vercel domain aliases as of this rebrand; the real cutover (DNS, `NEXT_PUBLIC_APP_URL`, `EMAIL_FROM`, Clerk custom domain) is an infrastructure task outside this repo.

**Update 2026-08-01:** `LEGAL_OWNER.email` in `LegalDocs.tsx` (the support inbox shown/linked throughout regulamin and polityka-prywatnosci, PL and EN) is now `support@pawelperfect.pl` — owner confirmed the `@pawelperfect.pl` inbox is live and monitored before this change went in. It had been deliberately left on `support@polutek.pl` during the initial rebrand specifically until that inbox existed; that condition is now satisfied.

**Update 2026-09-19 (legal entity changed to Kutashi Holdings LLC):** the `LEGAL_OWNER` constant in `LegalDocs.tsx` no longer describes a Polish sole proprietorship (`Paweł Perfect Inc.`, `ul. Złota 7/28`, NIP/REGON) — it's now the actual operating entity, `Kutashi Holdings LLC` (Seattle, WA; Wyoming LLC ID `2026-001487321`; EIN `99-4471268`), with contact `contact@kutashiholdings.com` / `+1 (307) 555-0148`. `LegalOwnerBlock`/`LegalOwnerBlockEn`'s letterhead card now renders "Wyoming LLC ID" + "EIN" instead of "NIP"/"REGON", and includes the email/phone lines directly in the card (previously contact info only appeared inline elsewhere in the doc body). This is a single shared component/data object, so the change applies identically across all four documents (`RegulaminContent`, `PolitykaContent`, `TermsContentEn`, `PrivacyContentEn`) — do not fork per-document owner data. The `leadIn` wording passed to the component still differs correctly by legal role: Regulamin/Terms (a sales-contract counterparty) say "Sprzedawcą serwisu www.kutashi.com jest" / "The Seller operating www.kutashi.com is" (domain updated 2026-09-20, see below), while Polityka Prywatności/Privacy (the GDPR data-controller role) keep "Administratorem serwisu www.pawelperfect.pl jest" / "The controller of your personal data is" — same entity, correct distinct legal terms per document. `OWNER.email` (now `contact@kutashiholdings.com`) is still the one address used for every inline complaints/support/account-deletion mailto link across all four documents.

**Update 2026-09-20 (all four legal documents' domain reference → kutashi.com):** at the owner's explicit request, every visible `www.pawelperfect.pl` site-address reference across all four legal documents was changed to `www.kutashi.com`, plus the static footer mark (`app/components/Footer.tsx`). This landed in three passes as the owner kept finding remaining spots, so the final state (not the history) is what matters for future agents:
- `LegalDocs.tsx`: `RegulaminContent`'s, `PolitykaContent`'s, and `TermsContentEn`'s `leadIn` strings (the three lines quoted just above) all read `www.kutashi.com`. `PrivacyContentEn`'s leadIn never named a domain ("The controller of your personal data is") so needed no change.
- All four route wrapper pages — `app/[locale]/regulamin/page.tsx`, `app/[locale]/terms/page.tsx`, `app/[locale]/polityka-prywatnosci/page.tsx`, `app/[locale]/privacy-policy/page.tsx` — hardcode `www.kutashi.com` directly in `metadata.title`, `metadata.description`, and the visible `<h1>`, and none of them import `APP_NAME` anymore. Before this, all four rendered their own title/heading via the `APP_NAME` brand constant (e.g. "Regulamin serwisu {APP_NAME}"), which is a *different* string from the in-body `leadIn` domain reference — `APP_NAME` resolves to `www.pawelperfect.pl` by design (see `lib/constants.ts`: an explicit 2026-08-01 owner decision, "used site-wide: page titles, legal docs, email subjects", matching the Stripe statement descriptor) and is completely unrelated code to the `leadIn` prop threaded through `LegalOwnerBlock`/`LegalOwnerBlockEn`. That distinction is exactly why the first pass (leadIn + footer only) and the second pass (page titles) each looked complete but weren't — check both places if this ever needs auditing again.
- **`APP_NAME` itself was intentionally left untouched** and still resolves to `www.pawelperfect.pl` everywhere else it's used (Navbar, other page titles, email subjects, etc.) — changing that constant is a full site-wide rebrand decision, not a legal-document content edit, and was never asked for. Do not "fix" the remaining `www.pawelperfect.pl` occurrences outside these four legal documents' body/title (e.g. `Navbar.tsx`'s aria-label, `BrandName.tsx`'s alt text, `sitemap.ts`'s default base URL, `EmailTemplateEditor.tsx`'s placeholder replacement) without a separate explicit request — they're the same underlying brand string by design, not stale leftovers. (This bullet used to also name `SecretProjectExperience.tsx`/`SecretProject2Experience.tsx` and `LanguageContext.tsx`'s `donationDescription` — both gone: the former two files were deleted with `/secretproject`/`/secretproject2` on 2026-09-22, and `donationDescription` was removed the same day as unused dead code, see below.)
- Do not assume `www.kutashi.com` should propagate to the Clerk/CSP hostnames in `lib/utils/security.ts`, `MAIN_CREATOR_SLUG`, `NEXT_PUBLIC_APP_URL`, or `EMAIL_FROM` — those remain real DNS/Clerk custom-domain/DB-lookup-key values untouched by this change, per the same reasoning already documented in the 2026-07-30 rebrand section above (this is a legal-document content edit, not an infrastructure cutover).
- **Fourth pass, same day:** `RegulaminContent`'s and `TermsContentEn`'s §2 body paragraph (`LegalDocs.tsx`) still opened with `{APP_NAME}` (→ `www.pawelperfect.pl`) — a *third* place this brand-vs-domain distinction bit, missed by both prior passes because it's plain body prose, not a `leadIn` or a page title/`<h1>`. Then, in the same round, the owner asked that the domain read in **uppercase and without the `www.` prefix** everywhere it appears in these documents. Both are now folded together: every visible occurrence of the domain across all four legal documents, all four route wrapper pages' `metadata.title`/`metadata.description`/`<h1>`, and the footer mark (`app/components/Footer.tsx`) reads the literal string `KUTASHI.COM` (no `www.`, all caps) — not a computed/templated value. `LegalDocs.tsx` no longer imports `APP_NAME` at all (its only two uses were these two now-hardcoded §2 sentences). `LEGAL_OWNER.email` (`contact@kutashi.com`, see the entity-rename note above) stays lowercase — the uppercase/no-`www.` request was about the domain-name reference, not the mailto address. If this domain ever needs to change again, grep for the literal `KUTASHI.COM` string (not `APP_NAME` or `www.kutashi.com`) across `LegalDocs.tsx`, the four `app/[locale]/{regulamin,terms,polityka-prywatnosci,privacy-policy}/page.tsx` files, and `Footer.tsx`.

**Update 2026-09-20 (permanent — legal entity renamed again, "one creator" framing genericized):** two more owner-requested, permanent edits to `LegalDocs.tsx`, distinct from the temporary block immediately below:
- `LEGAL_OWNER.name` changed from `'Kutashi Holdings LLC'` to `'Kutashi Yakimoto LLC'`, and `LEGAL_OWNER.email` changed from `contact@kutashiholdings.com` to `contact@kutashi.com`. Both fields are the single shared `LEGAL_OWNER` object (see the 2026-09-19 entity-change note above — "do not fork per-document owner data" still applies), so this applies identically across all four legal documents' letterhead card and every inline `OWNER.email` mailto link. Address, Wyoming LLC ID, EIN, and phone were left unchanged — only the legal name and contact email moved.
- `RegulaminContent()`'s and `TermsContentEn()`'s §2 opening sentence no longer describes the site as "an original video channel of one creator" (PL: "autorski kanał wideo jednego twórcy"; EN: "a single creator's video channel"). Both now describe it generically as a mono-channel — one, unified video channel — without naming or implying whose channel it is: PL now reads "{APP_NAME} to monokanał wideo — jeden, spójny kanał z materiałami wideo."; EN reads "{APP_NAME} operates as a single video channel — one, unified channel of video content." `PolitykaContent`/`PrivacyContentEn` never had this "one creator" framing (their §2-equivalent content is about data, not channel identity), so neither needed a change.

**Update 2026-09-20 (permanent, EN Terms-only follow-up — drop remaining "channel" language):** `TermsContentEn()`'s §2 sentence above was revised again, this time to drop the word "channel" entirely rather than genericize it: it now reads "KUTASHI.COM is an online platform presenting video content." The PL Regulamin equivalent ("monokanał wideo") was intentionally left as-is — this pass was scoped to the EN Terms document only. The same function's §4 second paragraph ("People who already have active access to the Thank You Zone may additionally support the channel with any amount.") was also changed, `the channel` → `the project`, to match the rest of that paragraph's existing "the development of the project" / "the library of bonus content" wording now that §2 no longer frames the site as a channel. No other section of `TermsContentEn()` (§1, §3, §5–8) referenced "channel", so no further changes were needed there.

**Update 2026-09-22 (permanent — domain reverted KUTASHI.COM → WWW.PAWELPERFECT.PL everywhere, and PL/EN §2 parity fixed; supersedes the 2026-09-20 domain-change entries above):** at the owner's explicit request, every visible `KUTASHI.COM` occurrence across all four legal documents (`LegalDocs.tsx`'s `leadIn` strings and §2 body prose), all four route wrapper pages' `metadata.title`/`metadata.description`/`<h1>`, and the footer mark (`app/components/Footer.tsx`) was changed back to the literal string `WWW.PAWELPERFECT.PL` (uppercase, with the `www.` prefix — note this is a different literal casing than the pre-kutashi.com `www.pawelperfect.pl`/`APP_NAME` form documented above; it is not templated through `APP_NAME`, same as `KUTASHI.COM` wasn't). The four "Update 2026-09-20 … → kutashi.com" entries above are now historical — the domain name they describe changing *to* is exactly what this update changes back *from*. If this domain ever needs to change again, grep for the literal `WWW.PAWELPERFECT.PL` string across `LegalDocs.tsx`, the four `app/[locale]/{regulamin,terms,polityka-prywatnosci,privacy-policy}/page.tsx` files, and `Footer.tsx`. Separately, `RegulaminContent()`'s §2 opening sentence — which had read "{domain} to monokanał wideo — jeden, spójny kanał z materiałami wideo" since the 2026-09-20 "one creator" genericization entry above — was flagged by the owner as wrong and out of sync with `TermsContentEn()`'s §2, which already read "{domain} is an online platform presenting video content." (from the *next* 2026-09-20 entry above, EN-only at the time). PL now reads "WWW.PAWELPERFECT.PL to platforma internetowa prezentująca materiały wideo." — a direct translation of the EN sentence, not the monokanał wording. Both `RegulaminContent()`'s and `TermsContentEn()`'s §2 opening sentences are considered the source of truth going forward; keep them as direct translations of each other if either changes again.

**Update 2026-09-22 (major, owner-authored — Regulamin/Terms of Service completely rewritten to a non-commercial, donation-only model; `RegulaminContent()`/`TermsContentEn()` only, distinct from every entry above):** the owner pasted a full replacement Regulamin text verbatim in the project chat and asked it to be implemented as-is, translated faithfully to English, with nothing guessed or drafted independently. **This is a fundamentally different legal/business framing than everything documented above it in this section, not just a wording tweak:**
- **Legal identity changed from an LLC back to a natural person.** §1 no longer names `LEGAL_OWNER` (`Kutashi Yakimoto LLC`, a Wyoming LLC) at all — it names "Paweł Perfect, a private individual" with a Warsaw correspondence address (`ul. Złota 75, 00-819 Warszawa`) and a *different* contact email, `support@pawelperfect.pl` (not `OWNER.email`/`contact@kutashi.com`). This is deliberately scoped to Regulamin/Terms only: a new local `REGULAMIN_CONTACT_EMAIL`/`TERMS_EN_CONTACT_EMAIL` constant (`= 'support@pawelperfect.pl'`) is used for every mailto link in these two documents instead of the shared `OWNER`/`LEGAL_OWNER`, and `LegalOwnerBlock`/`LegalOwnerBlockEn` (the Kutashi Yakimoto LLC letterhead card with Wyoming LLC ID/EIN/phone) are no longer called from either document — plain numbered-list prose instead, matching the exact structure of the owner's pasted text. **`PolitykaContent()`/`PrivacyContentEn()` (Privacy Policy) were deliberately left untouched** and still name Kutashi Yakimoto LLC via `LEGAL_OWNER`/`OWNER.email` (`contact@kutashi.com`) as data controller — the owner's message and the coordinator's brief were scoped to "regulamin" only. **This is now a real, live inconsistency between the two documents** (different legal entities, different emails) that a future agent should flag rather than silently "fix" one way or the other. **Update 2026-09-22:** asked directly, the owner said to leave Polityka Prywatności as-is for now ("na razie polityke zostaw na później") — this is a deliberate deferral, not an oversight, so don't re-raise it unprompted; the inconsistency is known and accepted for the time being. Revisit only if the owner brings it up again.
- **Business model changed from "sale of digital content" to "free service, optional non-refundable donations that grant nothing."** The new §2.1 states explicitly: "All content is available to all users on the same terms, regardless of whether they have given support." §4 ("Wsparcie Twórcy"/"Supporting the Creator") states support is a gift/donation, not a price or payment for anything, grants no access/features/benefits, creates no obligation on the Creator, and — critically — is **non-refundable** (except mistaken payments or unauthorized card use), with **no invoices issued** ("Support is not payment for a sale"). This entirely removes the old "one-time payment → lifetime Thank You Zone access" framing, the Merchant of Record / Lemon Squeezy language from the 2026-09-21 revert above, the per-currency patron-threshold pricing language, and the entire former §5–8 apparatus (payments/prices/invoices, right of withdrawal under art. 38(1)(13), out-of-court consumer dispute resolution) — none of that exists in the new 7-section document (§1 Who runs the service, §2 What you'll find + technical requirements, §3 Account and usage rules, §4 Supporting the Creator, §5 Complaints, §6 Personal data, §7 Changes and governing law), because there is no longer a "sale" for Polish/EU consumer-sale law to apply to.
- **This document rewrite was implemented as literal text replacement only — no code behavior was changed in this pass.** The owner's message also said (same message, appended after the pasted Regulamin text): "ze strony dla zalogowanych usun bramke platnosci i wstaw bramke napiwkowa, ma byc jasno napisane zgodnie z regulaminem ze jest to napiwek" (remove the payment gate from the page for signed-in users and put in its place a tip gate, clearly labeled per these Terms as a tip). **This was ambiguous at the time and deliberately not implemented in this pass** — see the next entry below for the scope-confirmation and the implementation that followed the same day.

**Update 2026-09-22 (follow-up, same day — payment gate removed, implements the item flagged above):** the owner confirmed, in response to a direct clarifying question, option (b): *"po zalogowaniu nie ma byc bramki oplatowej za wejscie do strefy thank you tylko od razu taka bramka napiwkowa z dowolna kwota jaka jest dla patroniow"* — after signing in there should be no payment gate for the Thank You Zone; instead, immediately the same free-amount tip gate patrons already saw. Implemented as a real access-control change (not just copy): `checkVideoAccess()` now grants `PATRON`-tier videos to any signed-in, non-deleted user exactly like `LOGGED_IN` (§4.4), and `DonationBox.tsx` always renders the "Bramka Napiwkowa" tip-jar variant (§4.10). `PatronGrant`/`fulfillPayment()` themselves were **not** touched — support still creates a `PatronGrant` row, it's just no longer read by the access gate (§4.1–§4.2). The Polityka Prywatności/Regulamin legal-entity mismatch flagged in the entry above is still open and unrelated to this follow-up — still needs the owner's call. Also still open: whether the art. 38(1)(13) withdrawal-consent checkbox (§4.10) is still needed now that support is explicitly not a sale.

**Update 2026-09-22 (Regulamin/Terms shortened — §5 Complaints and §6 Personal data removed, §7 renumbered to §5):** the owner asked to delete the old §5 "Reklamacje"/"Complaints" and §6 "Dane osobowe"/"Personal data" sections from `RegulaminContent()` and `TermsContentEn()`, reasoning that (1) "Reklamacje" (a consumer complaint/warranty-claim term) no longer fits a non-commercial free service with no sale to complain about, and (2) the personal-data-controller statement is already covered by the dedicated Privacy Policy document, so repeating it in the Terms is redundant. Both sections were deleted outright (not flagged/hidden) in both languages, and the old §7 "Zmiany regulaminu i prawo właściwe"/"Changes to the Terms and governing law" was renumbered to §5 — both documents now end at §5 (§1 Who runs the service, §2 What you'll find + technical requirements, §3 Account and usage rules, §4 Supporting the Creator, §5 Changes and governing law). §5's own wording was left unchanged (only its `nr` prop changed) — the owner's message pasted its text back for context, not as a wording-change request. Confirmed via grep that no other part of either document cross-references "§5"/"§6"/"§7", "Reklamacje" or "Dane osobowe"/"Personal data" after the removal. `PolitykaContent()`/`PrivacyContentEn()` (the Privacy Policy) were not touched — they keep their own separate 8-section structure (§1 Administrator danych/Data controller … §8 Zmiany polityki/Changes to this policy), which is unaffected by anything in this entry; the Regulamin's removed §6 pointed readers there, and that pointer is simply gone now rather than needing to go anywhere else. `REGULAMIN_CONTACT_EMAIL`/`TERMS_EN_CONTACT_EMAIL` are still used elsewhere in both documents (§1, §3, §4), so neither constant went unused.

**Update 2026-09-22 (accuracy fix — Regulamin/Terms §2 and §4 corrected to match the actual access model, PL/EN kept as direct translations):** the owner flagged that §2's "Niektóre funkcje, np. komentowanie, wymagają bezpłatnego konta" (only *features* like commenting need an account) was stale now that the access-gate removal above made this a general statement of intent rather than a description of what's actually gated — some **videos**, not just comments, also require sign-in (`LOGGED_IN`/`PATRON`-tier videos), since only `PUBLIC`-tier videos are open to guests; no video has ever required *payment*, which is the distinction §2 needed to draw and wasn't. `RegulaminContent()`'s and `TermsContentEn()`'s §2 were both rewritten to state plainly that access to content never requires payment, and separately that some video content and some features (e.g. commenting) require a free account while other content is available without signing in — replacing the old "all content for all users on the same terms" + "some features need an account" bullets, which conflated "never requires payment" with "never requires an account" and were literally true only for the payment half. Same pass also fixed a related inaccuracy in §4 ("Wsparcie Twórcy"/"Supporting the Creator") item 1, which said "Każdy, z kontem lub bez, może przekazać Twórcy dobrowolne wsparcie" / "Anyone, with or without an account, may give the Creator voluntary support" — factually wrong, since `useCheckoutFlow.ts`'s `submit()` requires a signed-in `userId` (opens the sign-in modal otherwise) before any donation can proceed on all three checkout surfaces (`DonationBox`, `SecretPledgeBox`, `SecretPledgeBox2`). Both language versions now state a free account/sign-in is required to give support. As with every PL/EN pass on this file, both sentences are kept as direct translations of each other — re-check both together if either changes again. `PolitykaContent()`/`PrivacyContentEn()` were not touched (no equivalent claims there). Still open, not addressed in this pass: `/secretproject`'s "pledge unlocks reward" copy is stale for the same underlying reason (flagged in the entry above) — the video it describes as unlocked-by-pledge is already unlocked for any signed-in user.

**Known test debt from this change:** several existing unit/integration tests assert the pre-2026-09-22 model (`PATRON` tier requires an active `PatronGrant`, guests see `PATRON_REQUIRED`) — e.g. `tests/unit/patron-access-source-of-truth-v2.test.ts`, `tests/unit/patron-grant-truth.test.ts`, `tests/unit/access-source-of-truth.test.ts`, `tests/unit/media-source-safety.test.ts`, `tests/unit/api/media-source-route.test.ts`, `tests/unit/api/access-route.test.ts`, `tests/unit/api-contracts.test.ts`, `tests/integration/launch-candidate-critical-path.test.ts`, `tests/unit/security/launch-security-boundaries.test.ts`, `tests/unit/components/channel/DonationBox.test.tsx`, `tests/unit/components/access-lock-overlay-*.test.ts`, `tests/unit/playback-plan-state-messaging.test.ts`, and the various `lib/modules/comments/` use-case tests that branch on `PATRON_REQUIRED`. `tests/unit/modules/access/check-video-access.use-case.test.ts` (the direct unit test of the changed use case) was updated to match the new behavior; the rest were not, since this environment has no `node_modules`/test runner to verify changes against — a future agent should update or delete the stale assertions rather than assume a red run means the production code regressed. Also now stale from the same-day withdrawal-checkbox removal (§4.10): `tests/unit/hooks/useCheckoutFlow.test.tsx` and `tests/unit/components/channel/DonationBox.test.tsx` still reference `isWithdrawalAcknowledged`/`acceptWithdrawal`/`pleaseAcceptWithdrawal`, none of which exist anymore.

**Update 2026-09-22 (audit + removal — `/secretproject`/`/secretproject2` deleted, Polityka Prywatności payment-purpose wording fixed, dead translation keys removed):** the owner asked for a deeper audit of whether the whole repo actually matches the free-access/voluntary-donation model above. Findings and what was done:
- **`/secretproject` and `/secretproject2` were deleted outright** (owner's explicit call once the finding was reported, not a judgment call made unilaterally). The audit found they still behaved as a real paywall: `SecretPledgeBox`/`SecretPledgeBox2` forced a **readOnly fixed-price amount field** and copy like "the material stays locked until you back the campaign — one pledge unlocks it" for any viewer without a legacy `PatronGrant` row, even though the reward video (PATRON tier) was already playable for any signed-in viewer via `PremiumWrapper` per §4.4 — because `loadSecretProjectViewerIsPatron()` (the old `lib/modules/campaign/secret-project-funding.ts`) kept computing "patron" from `PatronGrant`, not sign-in state. `CheckoutModal`/`CheckoutSummaryPanel` would even tell such a viewer "Zostajesz Patronem Projektu" / "Dożywotni dostęp do Strefy Fenkjuu" — a real, live inconsistency with the "wsparcie to nie biznes" model, not just stale copy. See the removal bullet in §"Current UI/runtime baseline" above for what was deleted. `CheckoutModal.tsx`/`CheckoutSummaryPanel.tsx` themselves were **not** touched — `DonationBox` is their only remaining caller and always passes `viewerIsPatron={true}`, so their `!viewerIsPatron` branches are now fully dead code, left in place under the same "kept for a small diff if this ever needs to come back" reasoning `DonationBox.tsx` already documents for its own dead branch — do not delete them as a side effect of an unrelated change without re-checking this note.
- **`PolitykaContent()`/`PrivacyContentEn()`'s payments data-table row fixed** (`LegalDocs.tsx`, `DATA_ROWS`/`DATA_ROWS_EN`): the "Płatności"/"Payments" row's stated purpose used to include "nadanie dostępu"/"granting access" and its data column said "status dostępu"/"access status" — stale since the 2026-09-22 access-gate removal (§4.4) made payments grant nothing. Renamed the row "Wsparcie (płatności)"/"Support (payments)", purpose now "obsługa dobrowolnego wsparcia, potwierdzenia i obowiązki podatkowe"/"processing voluntary support, confirmations and tax obligations", data column now "status płatności"/"payment status". The broader §2 legal-basis paragraph and the Kutashi Yakimoto LLC vs. Paweł Perfect entity mismatch (data controller in Polityka vs. seller in Regulamin) were **not** touched — the owner has explicitly deferred that one "na później" (see the 2026-09-19/22 entity-change notes above); don't re-raise or "fix" it unprompted.
- **Dead translation keys removed** from `LanguageContext.tsx` (both `pl`/`en`, confirmed zero references anywhere via grep before removal): `becomePatron`, `becomePatronToComment`, `paywallUnlock`, `donationDescription`, `patronOnly`, `unlockedStatus`, `paywallText`, `paywallAction`, `topSecret`, `available`, `publicStatus`. All were leftover copy from the old pay-to-unlock model (e.g. `donationDescription` said support grants "dostęp do Strefy Fenkju") that nothing in the app actually rendered anymore.
- **Left alone, flagged but not fixed:** `AccessLockOverlay.tsx`'s `PatronScene`/`isPatron` branch (keyed on the `PATRON_REQUIRED` access reason) is unreachable dead code since `checkVideoAccess()` never emits that reason anymore (§4.4) — not removed in this pass since it's a large, CSS-heavy branch and removing it wasn't part of what the owner asked for; a future cleanup can take it out once/if that's explicitly wanted. The ~10 stale test files listed just above are still stale — same reasoning, no test runner available in this environment to verify a fix against.

## TEMPORARY CHANGES REGISTRY (test-mode only — revert before real launch)

**Any AI agent working in this repo:** if the owner ever says "cofnij wszystkie zmiany tymczasowe" / "revert all temporary changes" (or the English equivalent), this section is the complete, authoritative list. Every entry below is a single `const TEMP_* = true` boolean guarding a small, self-contained code branch — flip each flag to `false` (or delete the flag and the branch/conditional that reads it, restoring the code beneath/alongside it) and nothing else needs to change. Do not go looking for other "temporary-feeling" code outside this list — if it isn't listed here, it isn't part of this batch. When a future change adds another temporary flag, add it to this list in the same PR; when a flag is reverted, delete its entry from this list in the same PR.

**Update 2026-09-20 (TEMPORARY — test-mode UI changes, revert before real launch):** at the owner's explicit request, marked in the request itself as temporary and test-only ("to sa zmiany tymczasowe... żeby przyszłe AI mogło to cofnąć"). None of the flags below touch data, access control, or payments.

1. `app/components/Navbar.tsx`: a module-level `const TEMP_HIDE_NOTIFICATIONS_BELL = true` flag. The signed-in `<NotificationsMenu />` (the "dzwoneczek"/bell) render is wrapped in `TEMP_HIDE_NOTIFICATIONS_BELL ? null : (...)` inside the existing `isSignedIn` branch (the `isLoaded`-signed-out and skeleton branches are untouched). **To revert:** flip the constant to `false`, or delete the flag and the wrapping conditional so the JSX renders unconditionally again exactly as before. (The sibling `TEMP_HIDE_LANGUAGE_SWITCHER` flag that used to live alongside this one was reverted 2026-09-21 — see below — and the flag/conditional were deleted outright rather than left in place, since there was no reason to expect the language switcher would need hiding again.)
2. `lib/i18n/server-language.ts`: a module-level `const TEMP_FORCE_ENGLISH = true` with an early `if (TEMP_FORCE_ENGLISH) return "en";` at the top of `resolveInitialLanguage()`, before the DB/cookie/geolocation/Accept-Language chain described in the function's own docstring (§4.11 of this file). This makes the root `/` redirect (`app/page.tsx`) always land on `/en`, and makes every fresh server-rendered page default to English, while leaving the underlying 5-step resolution logic completely intact and unreached. **To revert:** flip the constant to `false`, or delete the flag and the early-return line — the original priority chain immediately below it is untouched and needs no other changes.
3. These flags are the *only* changes in this registry; do not extend "temporary test mode" to any other component without a fresh explicit request, and do not let this pattern (a `TEMP_*` boolean short-circuiting a component or function) become a general convention — it exists here only because the owner explicitly asked for an easy, obviously-temporary, single-file revert per change.

**Update 2026-09-20 (reverted):** `app/components/BrandName.tsx`'s `TEMP_LOGO_AS_TEXT` flag (the temporary `KUTASHI.COM` text wordmark) was flipped back to `false` at the owner's request ("przywróć logo okularków ze zmian tymczasowych") — `BrandName` renders the permanent glasses-mark `<img src="/logo-glasses.svg">` again. The flag and its now-unreached text-wordmark branch were left in place rather than deleted, in case the wordmark needs to come back again; if it's never revisited, a future cleanup can remove the dead branch. `tests/unit/components/brand-name.test.tsx` was restored to its original `<img>`-based assertions (accessible image, `alt="www.pawelperfect.pl"`, `src="/logo-glasses.svg"`) to match.

**Update 2026-09-21 (reverted):** `app/components/Navbar.tsx`'s `TEMP_HIDE_LANGUAGE_SWITCHER` flag was removed at the owner's request — the PL/EN language-switcher pill in the navbar now renders unconditionally again, exactly as before it was hidden. Unlike the other reverted flag above, both the constant and its wrapping `{!TEMP_HIDE_LANGUAGE_SWITCHER && (...)}` conditional were deleted outright (not just flipped to `false`), since the switcher isn't expected to need hiding again. `TEMP_HIDE_NOTIFICATIONS_BELL`, the sibling flag that used to sit next to it, is untouched and still hides the bell — see item 1 above.

**Update 2026-09-22 (made permanent, not just reverted):** the `TEMP_HIDE_TERMS_EN_SUMMARY` flag (item 3 above in earlier versions of this list, `app/components/legal/LegalDocs.tsx`) hid only the English Terms' "W skrócie"/summary block behind a flag while the Polish Regulamin's own `<LegalSummary>` call stayed unconditionally rendered — so the two languages disagreed on whether a summary block existed at all. The owner's 2026-09-22 request confirmed the no-summary state is correct for *both* languages, not just a temporary EN-only test state: the flag, its conditional, and the `<LegalSummary>` call were deleted from `TermsContentEn()`, and `RegulaminContent()`'s own (unconditional, always-shown) `<LegalSummary>` call was deleted outright too — no flag, since there is no scenario where the Polish summary should come back while the English one stays gone. Neither `PolitykaContent()` nor `PrivacyContentEn()` (the Privacy Policy documents) had this issue or were touched — they keep their own `<LegalSummary>` blocks in both languages, consistently.

**Update 2026-09-21 (reverted, unregistered stray commit):** commit `f65709b` ("Update LegalDocs.tsx", 2026-09-22 00:04 local) was pushed directly to `main` via the GitHub web editor — not through a PR or this registry — and added "Lemon Squeezy, LLC" as a Merchant of Record to the **Polish** Regulamin only (`RegulaminContent()`'s §1/§4/§5, restructuring §6–8), while leaving `TermsContentEn()` untouched. That left the two language versions naming different sellers: Kutashi Yakimoto LLC (EN, and the pre-existing PL) vs. Lemon Squeezy as Merchant of Record (PL only). It was never added to this registry (no `TEMP_*` flag — it directly edited the JSX) because it wasn't flagged as temporary when it landed; the owner asked for it to be undone once he noticed the mismatch. Reverted via `git revert f65709b`, which restores Kutashi Yakimoto LLC as the plain seller in both languages (no Merchant of Record), the original §4–8 wording (withdrawal-right waiver under art. 38 ust. 1 pkt 13, Stripe-only payments in §5), and "Strefa Fenkjuu" (the commit had swapped in the English "Thank You Zone" phrase for the Polish document). If Lemon Squeezy (or any other Merchant of Record) is reintroduced, do it identically in both `RegulaminContent()` and `TermsContentEn()` in the same change — the whole point of this note is that editing only one language silently breaks PL/EN parity.

**Update 2026-09-22 (TEMPORARY — "coming soon" premiere countdown placeholder for the whole PATRON tier):** at the owner's request, the "Strefa Fenkjuu"/"Thank You Zone" section label and locked-video overlays were replaced with a "Już wkrótce"/"Coming soon" placeholder plus a live premiere countdown, for **all** PATRON-tier videos uniformly (there is no per-video premiere-date field in the schema — this is a single hardcoded placeholder date shared across every patron video, not a real per-video release schedule). This is a pure display-layer gate — it does **not** touch `checkVideoAccess()`/`PatronGrant`; a signed-in user's actual access to PATRON-tier videos (§4.4) is completely unchanged. It only decides what the sidebar/lock-overlay UI *shows* for PATRON-tier videos, and additionally blocks the click-through/playback affordance in the two surfaces listed below (so the placeholder isn't just decorative — the video is not reachable from these surfaces while the flag is on).

1. `lib/temp-patron-coming-soon.ts` (new file): `export const TEMP_PATRON_COMING_SOON = true`, a hardcoded `PATRON_COMING_SOON_TARGET` `Date` constant (the placeholder premiere date/time — **update this constant directly whenever the real premiere date changes**, there is no admin UI for it), and `formatPremiereCountdown(isPl, target?, now?)` which renders `"PREMIERA ZA {d}D {h}H"` / `"PREMIERES IN {d}D {h}H"`.
2. `app/components/AccessLockOverlay.tsx`: `AccessLockState` gained a `"COMING_SOON"` member (not a real `PlaybackPlanStatus` — it's a display-only pseudo-state). When `state="COMING_SOON"`: the compact/thumbnail variant shows a `Rocket` icon with the live countdown as its label (amber/patron color theme, same `styles.patron` CSS as the real patron lock); the full/default variant renders a new `ComingSoonScene` (heading "Już"/"wkrótce" or "Coming"/"soon", plus the countdown line, no CTA button since there's nothing to unlock). The countdown label ticks over via a `setInterval` re-render every 60s while mounted.
3. `app/components/PremiumWrapper.tsx`: gained an opt-in `showPatronComingSoon?: boolean` prop (default `false`). When `TEMP_PATRON_COMING_SOON && showPatronComingSoon && effectiveTier === "PATRON"`, it renders `<AccessLockOverlay state="COMING_SOON" .../>` immediately, before any of the real access/loading branches — this is checked early specifically so it doesn't wait on (or get overridden by) the real playback-plan fetch. **This is deliberately scoped to `Hero.tsx`'s main-feed player only** (the only call site that passes `showPatronComingSoon`) — `/admin/videos/[id]/page.tsx` (admin needs to actually preview/diagnose patron videos) and `/secretproject`/`/secretproject2` (their own separate campaign narrative around the "locked reward" video) both still call `PremiumWrapper` without this prop and are unaffected.
4. `app/components/channel/SidebarPlaylist.tsx`: the PATRON section header (both the loading-skeleton and real-render paths) reads `patronSectionTitle`, which resolves to "Już wkrótce"/"Coming soon" while the flag is on. Every video rendered inside that section is passed `forceComingSoon=true` into `renderVideoItem()`, which — only for those items, only while the flag is on — forces `lockState = "COMING_SOON"` regardless of the viewer's real `hasAccess`, and suppresses the normal access badge. The public/logged-in sections and the error-fallback rendering path (`fallbackItems`, used only when the sidebar-layout API call fails) are untouched.

**To revert:** flip `TEMP_PATRON_COMING_SOON` to `false` in `lib/temp-patron-coming-soon.ts` — every branch above reads that single constant, so this alone restores the pre-2026-09-22 "Strefa Fenkjuu"/"Thank You Zone" behavior everywhere. The `showPatronComingSoon` prop and `"COMING_SOON"` state can stay in place harmlessly if left in code (they're inert once the flag is `false`), or be deleted for a full cleanup.

**Update 2026-09-24 (still temporary — working prototype tweaks, not the target architecture):** at the owner's request, this same flag-based mechanism was tweaked to demo two things while the real per-video design (see `docs/tickets/ready/COMING-SOON-PREMIERE-COUNTDOWN-001.md`, filed the same day) is scheduled as follow-up work:
   - `formatPremiereCountdown()` (`lib/temp-patron-coming-soon.ts`) now returns days/hours/minutes/**seconds**, and `AccessLockOverlay.tsx`'s countdown re-render interval dropped from 60s to **1s**, so the label visibly ticks.
   - `SidebarPlaylist.tsx`'s `renderVideoItem()` no longer calls `event.preventDefault()` on click for a `forceComingSoon` item — a "coming soon" video in the sidebar is now fully navigable, exactly like any other video card. Clicking it sets it as the current `?v=` video, and `Hero.tsx`/`PremiumWrapper.tsx` already render the same `ComingSoonScene` full-size purely from `video.tier === "PATRON"` (see `showPatronComingSoon` above) — no additional wiring was needed for the "big overlay in the main feed" behavior, just removing the click-block.
   - This is still keyed on `tier === "PATRON"` for the *whole* tier, not a single film with its own premiere date — that gap, plus a real `Video.premiereAt` field and a safe public DTO for an unpublished film, is exactly what the linked ticket scopes.

Current UI/runtime baseline as of 2026-07-14:

- Next.js 16 App Router with React 19 is the framework target.
- Clerk remains the identity backend, but public auth/account UI is custom/headless (`AuthModalProvider`, `AuthModal`, `UserMenu`, `AccountModal`) rather than default Clerk widgets.
- Public entry is a progressive app shell. Do not reintroduce a blocking splash/ENTER gate.
- The root `/` route redirects to a locale. Keep `app/loading.tsx` visually neutral. The home page (`page.tsx`) and its full-screen `SitePreloaderScreen` fallback (`loading.tsx`, formerly `HomePageSkeleton` — see §4.13) live under the `app/[locale]/(home)/` route group — not directly in `app/[locale]/` — because a `loading.tsx` placed at a segment applies as an outer Suspense boundary to *every* route nested under that segment, not just its own page. Legal docs, search and other `app/[locale]/*` routes have their own scoped `loading.tsx` (e.g. `LegalDocSkeleton` for `regulamin`/`polityka-prywatnosci`/`terms`/`privacy-policy`); do not add a bare `app/[locale]/loading.tsx` again or it will leak the video/playlist skeleton onto every sibling route. `ChannelHome` likewise reserves comment space before client mount without drawing a second skeleton; `EmbeddedComments` owns the actual comment-loading skeleton.
- Video switching uses a local CSS fade/slide around the player area. Do not reintroduce fullscreen iris/wipe transitions.
- `AppPreloadProvider` warms playback plans, posters and comments on intent. Playback plans are isolated by Clerk viewer/session plus video, stale requests are aborted, and `PremiumWrapper` fails closed unless the current viewer has an explicit `READY + canPlay + access.allowed + source + matching videoId` plan. Keep this cache in-memory; do not weaken its viewer scope or add persistent service-worker caching without a cache-safety design.
- `InstallAppMenu` provides install/add-to-home-screen affordances where the browser/platform supports them. **Update 2026-08-01:** its only mount point (the below-player action rail in `Hero.tsx`) was removed at the owner's request ("usuń przycisk ściągnij"/remove the download button) — the component still exists and works, it's just currently unmounted everywhere. Re-add `<InstallAppMenu />` somewhere if the install affordance needs to come back rather than rebuilding it.
- The public site has one application-grade VOD/PWA direction. Experimental theme/font routes (`/testujemy`, `/cyberpunk*`, `/glass`, `/testowy*`, `/eksperyment*`, `/t*`, `/czcionka*`) and their scoped CSS/assets were removed. Do not recreate parallel skins; prototype by evolving production components and semantic tokens.
- Public typography is self-hosted Geist Sans/Mono from the `geist` package. `font-sans`, `font-heading`, and `font-brand` intentionally resolve to Geist Sans; `font-mono` resolves to Geist Mono. Builds must not fetch remote fonts.
- As of 2026-07-30, the site logo (`app/components/BrandName.tsx`, used by `Navbar.tsx`) is the glasses/sunglasses graphic `public/logo-glasses.svg` rendered as a plain `<img>` (sized via a caller-supplied `h-*`/`w-auto` className, not `next/image` — a small static local vector gains nothing from the optimizer), replacing the earlier "POLUTEK.PL" text wordmark. `BrandName` no longer has a `shine` prop (it only ever applied to the old text rendering and had already gone unused before this change) — just `className` and `decorative` (`decorative` renders `alt=""` for the case where an enclosing `<Link>` already carries its own `aria-label="POLUTEK.PL"`, avoiding double-announcing the name to screen readers). The Space Grotesk Bold `font-brandLogo`/`--font-brand-logo` font (`app/fonts/space-grotesk-bold-latin.woff2`) that the old text wordmark used is still defined in `app/fonts.ts`/`tailwind.config.ts` but is no longer referenced anywhere — leave the font infra in place unless explicitly asked to remove it, in case the wordmark returns. Logo height is `h-[28px] md:h-[34px]` in the Navbar's `h-[38px]` slot; the mark was sized noticeably smaller on the first pass and read as too small, so don't shrink it back down without checking it still fills most of the navbar slot. The Navbar's sign-in/"ENTER" button (`Navbar.tsx`, `t.signIn`) is a fixed `w-[38px]` icon-only circle below `sm:` (matching the other 38×38 icon buttons already in the navbar, and roughly one language-switcher segment) instead of `px-3.5` padding around a hidden-on-mobile text label, which had made it visibly larger than its neighbors; it only grows to fit the "ENTER" text at `sm:` and up (`sm:w-auto sm:px-4`). `app/components/playlist/CheckoutSummaryPanel.tsx` does **not** use `BrandName` — it stopped importing it (commit "Checkout: drop the glasses mark, retitle the non-patron flow") in favor of its own inline `SupportHandDrawing` SVG (a hand-drawn heart-in-hand mark, `INK`/`SUPPORT_AMBER` hex constants at the top of that file); don't reintroduce `BrandName` there without checking that decision first.
- The public palette uses `--chan-*` tokens: blue (`--chan-blue`) is the primary action/navigation accent, while the vivid yellow-orange amber family (`--chan-amber`, `--chan-amber-bright` and related tokens) is the visible secondary accent for patron/support surfaces and selected warm highlights. Both accents should be recognizable across the public experience without turning every generic action amber. Public surfaces use cool neutral application cards, soft borders, consistent radii and layered ambient shadows. Avoid hard offset “3D” shadows and new literal accent hexes. `NajsIcon` remains unrelated shared icon infrastructure.
- **`/secretproject` and `/secretproject2` were removed outright on 2026-09-22** (owner's explicit request, "do wyjebania zupełnie", after a deeper audit of the free-access model found they still acted as a paid gate — see the "Update 2026-09-22 (audit + removal)" entry near the top of this file for why). Their pages (`app/[locale]/secretproject/`, `app/[locale]/secretproject2/`), components (`app/components/secretproject/`, `app/components/secretproject2/`), shared lib module (`lib/modules/campaign/`), public `middleware.ts` route entries, and their tests are all gone. `useCheckoutFlow.ts`/`CheckoutModal.tsx`/`CheckoutSummaryPanel.tsx` are unaffected (still used by `DonationBox`) — do not recreate a `SecretPledgeBox`-style fixed-price/"pledge unlocks the reward" surface without an explicit new request; it would reintroduce exactly the inconsistency that got these pages removed.
- The homepage/channel (`Navbar`, `Hero`, `ChannelHome`, `SidebarPlaylist`, comments, support box, player states and footer) is one responsive application shell suited to browser and installed PWA use. The **admin** shell is out of scope and keeps its approved scoped treatment; `/admin/payments` must retain its existing treatment.
- As of 2026-07-30, below the `lg` (1024px) breakpoint the video player is full-bleed edge-to-edge — no rounded corners, no border/card frame, no page gutter — matching the standard mobile video-platform pattern (phones are already narrow, so a decorative frame around the player is wasted space). `Hero.tsx`'s outer wrapper only gains the rounded/bordered/shadowed "application card" treatment at `lg:`; the featured-media `<div>` uses `-mx-4 md:-mx-6 lg:mx-0` to bleed out of the page container's own horizontal gutter on phone/tablet while staying contained in the card on desktop. **That `<div>` must stay at its default `width: auto` — never add `w-full`/a fixed width alongside the negative `-mx-*` margins.** Width and margin are independent in the CSS box model: with a fixed width, a negative margin only *shifts* the box (it shipped once anchored left with a gap on the right, see the 2026-07-30 follow-up fix) instead of stretching it to fill both edges — only `width: auto` lets the browser expand the box into the margin gap on both sides. `watch-actions.module.css`'s player-internal bezel border/outline (the `section:has(.polutek-vidstack-player) > div > div:first-child` selector) is flattened to a plain black edge below 1024px for the same reason — a decorative inner frame reads as a stray box once there's no outer card around it. That same selector also gets a phone-landscape safeguard (`max-width: 1023px and (orientation: landscape) and (max-height: 560px)`): a full-bleed 16:9 box sized from viewport width alone can end up taller than a short landscape viewport, so in that state the player is instead sized from height (`aspect-ratio` + `height: 88vh`) and centered/letterboxed rather than stretched edge-to-edge — don't remove this without re-checking phone landscape. That height is deliberately a plain `vh` value, not `dvh`/`calc()`: a CSS unit the browser doesn't recognize invalidates the *entire* declaration it appears in (silently reverting to the width-driven sizing this rule exists to prevent), so resist the urge to make it more precise with newer viewport units unless you can verify support. The same landscape query also switches `.metaRow` to `flex-direction: row` (creator info and action buttons side-by-side instead of stacked) and `.actionRail` to a plain non-wrapping flex row, since landscape phone widths (600-950px) have room the narrow portrait layout doesn't. As of 2026-07-30 this letterbox/reflow behavior is a secondary fallback only — the primary defense is `app/components/RotateDeviceGuard.tsx`, mounted globally in `app/layout.tsx` (inside `AuthModalProvider`, so `useLanguage()` is available), which blocks the entire view with a full-screen "rotate your phone to portrait" prompt on real phones in landscape rather than trying to reflow around it. It's a pure-CSS `@media` gate in `RotateDeviceGuard.module.css` — `(max-height: 560px) and (min-width: 561px) and ((hover: none) or (pointer: coarse))` — with no JS orientation detection, so it can't lag or miss a rotation event. As of 2026-07-30 this deliberately avoids the `orientation` media feature (some mobile browsers evaluate it against the device/screen orientation-lock state rather than the actual viewport box, which can disagree with what's on screen) in favor of a plain width > height comparison, and uses `(hover: none) or (pointer: coarse)` — not `and` — so a device that misreports just one of the two touch signals still trips the guard; the `max-height: 560px` clause excludes tablets (their landscape height is comfortably above that) and the `min-width: 561px` clause keeps the guard off in portrait. Keep both layers — the guard is what the user actually sees, but the graceful CSS underneath is what's briefly visible if a device's `hover`/`pointer` media features don't match as expected. `public/manifest.json`'s `orientation` is `portrait-primary` (was `any`) so an **installed/standalone PWA session on Android** genuinely locks to portrait at the OS level — the app window never rotates at all, no prompt needed. This is a real platform ceiling, not a gap to keep chasing: iOS Safari has never implemented orientation-lock for web content, standalone or not, so on iPhone (browser tab or installed) physical rotation will always resize the viewport regardless of anything this codebase does, and `RotateDeviceGuard`'s blocking prompt is the actual best available substitute there. The comments panel and the mobile "videos" tab panel in `ChannelHome.tsx` follow the same framing rule: no border/background/shadow/rounded card below `lg` (just a `border-t` divider and vertical padding, relying on the page's own gutter), full card treatment restored at `lg:`. The comments/videos tab switcher above them is a flat full-width underline tab bar (edge-to-edge via the same `-mx-4 md:-mx-6` bleed, each tab `flex-1`, active tab marked by a thin colored bottom bar in `--chan-blue`/`--chan-amber`) rather than a rounded pill segmented control — a full-bleed rounded-full pill reads wrong once stretched edge-to-edge, so don't reintroduce the pill/segmented-control style at full width. Everything else below the player (title, creator row, action buttons, description panel, comments, playlist) keeps the page's normal horizontal gutter — only the player and the tab bar bleed to the edge. The desktop two-column dashboard (`lg:` sidebar `aside`, support box) keeps its existing card framing; this change is mobile/tablet-only.
- Video player controls remain fully custom (`app/components/player/PolutekControls.tsx`) rather than Vidstack's stock layout. `PlayerLoadingState`, `AccessLockOverlay`, `PlayerErrorOverlay` and `PlayerStateFrame` own matching player states. As of 2026-07-14 the full login-required and patron-required locks (`app/components/AccessLockOverlay.tsx` / `.module.css`) are a `var(--chan-ink)`-based "premium dark panel" style, per explicit owner iteration on an earlier near-pure-black (`#0a0a0a`) version that read as too glaring/oversaturated: an animated `.aurora` layer (three independently drifting blurred blobs, deep `--chan-blue` for login, deep `--chan-amber`/`--chan-amber-strong` for patron, all mixed into `--chan-ink` rather than pure black) plus a diagonal `.sheen` gloss sweep and a subtle grain `.noise` overlay behind a centered `Lock`/`Gem` icon, a moderate-weight two-line uppercase headline (white + accent-colored word, `font-weight: 780`, tight but not extreme tracking) matching the site's `font-brand`/Geist Sans premium-editorial voice, a thin divider, and a minimal text-only CTA (thin line + tracked label) that brightens on hover/focus — no card, no button chrome. `AccessLockOverlay.tsx` uses `framer-motion` for a staggered mount choreography (icon spring-in, headline lines fade-up, CTA fades in last); the ambient background motion is driven by CSS `@keyframes` (not framer-motion `repeat: Infinity`) for performance. Compact thumbnail locks use the same premium-dark-panel tone but stay static (no motion, no `.aurora`/`.sheen`) for performance — many can mount at once in `SidebarPlaylist`/`ChannelVideoCard` — with a smaller icon and a two-line-clamped label so longer copy (e.g. "Strefa Fenkju") doesn't get crushed by an oversized icon. All motion must stop under `prefers-reduced-motion` (both the framer-motion entrance and the CSS ambient animation), and denied states must never keep a player mounted for an exit animation. Don't reintroduce the earlier pastel/illustrated ticket-and-thank-you-card design, pure-black/oversaturated glow, or card/button chrome without an explicit request; patron styling remains visible even when a guest must sign in first.

---

## 1. What This Product Is

Polutek.pl is a **single-channel VOD platform** for one creator. It is not a marketplace, not a multi-tenant SaaS, not a subscription service. Core facts:

- One channel, one catalogue of videos, one admin cockpit.
- **Update 2026-09-22: the service is non-commercial.** All video content is free for every signed-in user — there is no payment gate anywhere on the public site. `PATRON`-tier videos require sign-in only, exactly like `LOGGED_IN`-tier ones; a qualifying Stripe payment no longer grants anything a plain sign-in doesn't already give. Support is a voluntary, non-refundable donation ("Bramka Napiwkowa"/tip jar) that grants zero access or benefits. See §4.1/§4.4/§4.10 for the implementation and the Regulamin/Terms rewrite note near the top of this file for the legal framing driving it.
- Videos have three access tiers: `PUBLIC`, `LOGGED_IN`, `PATRON` — `PATRON` is kept as a distinct DB/schema value (for now) but is functionally identical to `LOGGED_IN` in `checkVideoAccess()`. Do not read tier-name equality as implying different access.
- Comments are visible to all; writing/reacting requires login (all three tiers behave the same way for comments now that PATRON no longer gates on payment).

---

## 2. Stack

| Layer | Technology |
|---|---|
| Framework | Next.js 16 App Router + React 19 (deployed on Vercel) |
| Database | Neon PostgreSQL via Prisma ORM |
| Auth/Identity | Clerk identity backend with custom/headless app UI — NOT patron authority |
| Payments | Stripe (webhooks → fulfillPayment → PatronGrant) |
| Video delivery | Cloudflare Stream primary; Mux/YouTube/Vimeo foundation exists where supported |
| Email | Resend |
| Storage | Vercel Blob legacy thumbnails/media; Cloudflare R2 planned/current migration target |
| Rate limiting | Upstash Redis / Vercel KV |
| Crons | Vercel Crons (`vercel.json` `crons` array) |
| UI | Tailwind CSS + shadcn/ui + Geist + semantic `--chan-*` application tokens |
| Tests | Vitest; Playwright scaffolding for E2E smoke where browsers/env are available |

---

## 3. Module Map

```txt
lib/modules/
  access/           # Video access policy (checkVideoAccess, PlaybackPlan)
  audit/            # Audit event recording
  channel/          # Home/channel content loading
  email/            # Email repository, broadcast use cases, Resend adapter
  media/            # Thumbnail resolution, storage (S3/R2 presigned URLs), thumbnail HTTP response
  patron/
    application/    # grant-patron, revoke-patron, recalculate-patron-status use cases
    domain/         # patron DTOs, errors, policy
    infrastructure/ # PatronRepository (listActiveGrants, createGrant, revokeActiveGrants…)
  payments/         # Payment recording, fulfillPayment (canonical replay-safe path)
  playback/
    application/    # playback.service — resolves playable sources for a video
    domain/         # playback DTOs, playback policy, primary playable asset
    infrastructure/ # cloudflare signed playback token service
  users/
    application/    # admin user details, patron read model, sync-user use cases
    domain/         # user DTOs, errors, policies
    infrastructure/ # UserRepository
  shared/           # AppContext, Actor, result types, db helpers

app/                # Next.js App Router pages and API routes
app/components/     # Public app shell, player, auth UI, comments, preload and channel UI
app/api/            # API routes
app/admin/          # Admin panel pages and components
app/api/admin/      # Admin API routes
app/api/cron/       # Vercel Cron handlers (auth via CRON_SECRET bearer token)
app/api/media/      # Media proxy — only safe playback path for blob/legacy video
app/api/webhooks/   # Stripe, Clerk, Resend, Cloudflare/Mux webhook handlers

prisma/
  schema.prisma     # Single source of schema truth
  migrations/       # Never edit existing migrations; always add new files
```

---

## 4. Critical Invariants — Read Before Editing

**Update 2026-09-26 (schema/migration drift reconciled):** an audit found `schema.prisma` had silently diverged from what the full migration history actually builds — 7 indexes and several nullable/default changes declared in the schema with no migration ever creating them, plus two `Notification*` foreign keys missing `ON UPDATE CASCADE` that every other relation has. Confirmed via a from-scratch `prisma migrate deploy` + `prisma migrate diff` replay (no production DB access in this environment) and fixed with three new, additive-only migrations: `20260926120000_add_missing_schema_indexes`, `20260926120001_reconcile_notification_fk_onupdate`, `20260926120002_reconcile_schema_nullable_default_drift` (the last one backfills any pre-existing NULL `VideoPlaybackSession.maxProgressMs` to `0` before tightening it to `NOT NULL`, since `playback.service.ts`'s `videoPlaybackSession.create()` calls have always omitted that field in reliance on a DB default that didn't actually exist). A from-scratch replay now produces zero diff against `schema.prisma`. Full writeup: `docs/audit/2026-09-26-prisma-migration-drift-and-timestamp-audit.md` (also covers a separate, no-code-change audit of duplicate migration timestamps — `PRISMA-MIGRATION-TIMESTAMP-DEDUP-001` — which remains a historical/documentation finding only, since renaming an already-applied migration folder would break `_prisma_migrations` tracking on any environment that has it applied).

### 4.1 Patron Status Source of Truth

**`PatronGrant` table is the sole source of patron/supporter bookkeeping** — but as of 2026-09-22 it **no longer gates video access** (see §4.4). It still exists, is still created by `fulfillPayment()`, and is still what any "is this user a supporter" read (admin diagnostics, email audience, decorative UI badges) should query. A user is recorded as having supported iff:

```prisma
patronGrants: { some: { revokedAt: null } }
```

- `User.isPatron`, `User.patronSince`, `User.patronSource` fields **do not exist** (removed in migration `20260630000000_remove_legacy_user_patron_cache`).
- Never write patron status to the `User` table. Never read it from there.
- `recalculatePatronStatus()` is a pure read — it computes status from active grants, no writes.
- DTO/UI fields named `isPatron` may exist only as derived/decorative values computed from grants or admin role.
- **Do not reintroduce a `PatronGrant`/`getPatronStatus()` check inside `checkVideoAccess()`** without an explicit owner decision to revert the 2026-09-22 non-commercial model — that would silently repaywall content the current Regulamin promises is free.

### 4.2 Patron Grant Lifecycle

```txt
Stripe webhook (signature verified)
  → record StripeEvent
  → record Payment (financial fact)
  → Patron eligibility policy (amount ≥ threshold)
  → fulfillPayment() — canonical, replay-safe
      → creates PatronGrant
      → syncs Clerk metadata (cache only)
      → sends confirmation email
```

`fulfillPayment()` in `lib/modules/payments/` is idempotent and replay-safe. Always use it for payment fulfillment, never issue manual `updateMany({ status: 'SUCCEEDED' })` shortcuts. **This lifecycle is unchanged by the 2026-09-22 access-gate removal** — a qualifying payment still creates a `PatronGrant` record (useful supporter bookkeeping/decorative badge), it just no longer unlocks anything that sign-in alone didn't already unlock.

**Return-page fast path (added 2026-07-21):** the webhook remains the reliability backbone, but `getOwnedPaymentStatus()` (`lib/modules/payments/application/get-payment-status.use-case.ts`, backing `GET /api/payments/[paymentId]`) no longer only reads the local `Payment.status`. When the caller's own payment (ownership already enforced by the `{ id, userId }` lookup) is still `PENDING` and has a `stripeIntentId`, it retrieves that PaymentIntent directly from Stripe and, if Stripe already reports `succeeded`, calls `fulfillPayment()` right there — the same canonical, replay-safe path, never a manual status write — before responding. This is what lets `DonationBox.tsx`'s post-checkout success screen resolve near-instantly instead of only after the webhook lands; the polling loop there does an immediate check on mount rather than waiting for the first interval tick. Any Stripe/network failure in this path is swallowed and falls back to the normal webhook-driven polling — it is a best-effort fast path, not a new source of truth.

**Return-page message trusts Stripe's redirect signal.** `DonationBox.tsx` reads Stripe's own `redirect_status` off the return URL: `succeeded` (or the param being absent on a plain card confirmation) shows the full thank-you immediately; `failed` shows a red failure header (`CheckoutModal.tsx` renders a red ✕ + "something went wrong" for `FAILED_CANCELED`/`REFUNDED_DISPUTED`); genuinely async `pending`/`processing` methods get a neutral processing screen until the background reconciliation confirms. The reconciliation loop only ever *upgrades* a processing screen to success — it must never downgrade an already-shown success message, and no raw `PaymentUiStatus` enum or "webhook" jargon may reach the UI. Closing the success screen (X or "back to site") after a succeeded payment does a full `window.location` reload to the clean URL so every server-rendered surface (Patron badge, `viewerIsPatron` video locks, support-box copy) comes back reflecting the freshly-granted access at once — do not downgrade this to a soft `router.replace`, which leaves stale non-patron server state and cached playback plans.

### 4.3 Video Playback Security

- **Never expose `videoUrl` to the public frontend.** Use `PublicVideoDTO` for all public-facing data.
- `/api/media/[...path]` is the only public playback path for blob/legacy videos.
- `PlaybackPlan` from the access module gates all player mounting: `READY` → mount player, any denied state → locked placeholder.
- Never mount a player, fetch streams, request tokens, resolve provider playback, or log views for a denied plan.
- `isLegacyPrivatePlaybackFallbackAllowed()` from `lib/modules/playback/domain/playback-policy.ts` always returns `false` — do not bypass it or check `ALLOW_LEGACY_PRIVATE_FALLBACK` env directly.
- **Never CDN-cache `/api/media-source` responses** (no `s-maxage`/`public` Cache-Control). The response carries a per-viewer `playbackSessionId` bound to the requester fingerprint.

### 4.4 Access Checks

Access is checked via `checkVideoAccess()` in `lib/modules/access/`. Actor type comes from `getActorFromAuth()`.

**Update 2026-09-22 (PATRON tier no longer payment-gated):** `checkVideoAccess()` no longer reads `PatronGrant`/`getPatronStatus()` at all. For any signed-in, non-deleted local user, `LOGGED_IN` and `PATRON` tiers are handled identically — both just `return ok({ hasAccess: true })`. Guests still get `LOGIN_REQUIRED` for both tiers (never `PATRON_REQUIRED` — that reason string still exists in the type/DTOs for now but is no longer emitted anywhere; treat it as dead/reserved, not a live state). This mirrors the owner-authored Regulamin/Terms rewrite (see the "major" 2026-09-22 entry above): all content is free for every signed-in user, and support is a voluntary donation that grants nothing. The same LOGIN_REQUIRED-not-PATRON_REQUIRED change was mirrored client-side in `PremiumWrapper.tsx`, `AccessLockOverlay`'s guest-facing consumers (`SidebarPlaylist.tsx`, `ChannelVideoCard.tsx`), so no surface still shows "become a Patron" copy to a guest — logging in is presented as sufficient everywhere. Comment read/write gating (`lib/modules/comments/`) needed no change: it already branches on `checkVideoAccess()`'s `hasAccess` boolean, which now cascades the fix automatically.

### 4.5 Clerk Is Identity Only

Clerk provides user identity (userId, email, name). It does not control patron access. Clerk metadata is a sync cache/UI hint — the database is always authoritative.

### 4.6 Email Audience

`lib/modules/email/infrastructure/email.repository.ts` filters patron audience via `patronGrants: { some: { revokedAt: null } }`. Do not filter by `User.isPatron`.

### 4.7 Video Subtitle Tracks

- `Video.subtitleUrlPl` and `Video.subtitleUrlEn` are optional URL fields for WebVTT subtitle files.
- The playback service builds `textTracks` in the `PlaybackPlan.player` from these fields automatically.
- Set them via the admin video create/edit form or API. The `VideoPlayer` component consumes `textTracks`.
- Managed subtitle upload/validation/hosting is not complete; see issue #1219 before expanding this.

### 4.8 Thumbnail Display Path

**Rollout status 2026-09-26: not live yet.** The code is merged, but production still serves thumbnails through the proxy and uploads to Blob. The bucket env vars are set in Vercel, so the next deploy activates R2 uploads. If the R2 token turns out not to cover the thumbnail buckets, the upload routes fall back to Blob instead of failing. Direct public serving is still blocked: someone has to enable the bucket's `r2.dev` URL in the Cloudflare dashboard. The connector can't, and a custom domain isn't possible because the `pawelperfect.pl` DNS is at home.pl. After that, set `NEXT_PUBLIC_R2_PUBLIC_HOST`, redeploy and run the migration script. The exact current state and next steps are in `docs/tickets/ready/MEDIA-THUMBNAILS-R2-MIGRATION-001.md` → "Rollout status"; read it before touching this.

**Update 2026-09-26 (MEDIA-THUMBNAILS-R2-MIGRATION-001 — thumbnails on Cloudflare R2):** storage is split across two R2 buckets (helpers in `lib/modules/media/domain/r2-thumbnail.ts`, S3 client in `lib/modules/media/infrastructure/r2-thumbnail-storage.client.ts`):

- **Private bucket** (`CLOUDFLARE_R2_BUCKET_THUMBNAILS_PRIVATE`, `polutek-thumbnails-private`) — every admin upload lands here (`cover-upload` route, default-thumbnail setting route), drafts included, under a content-hashed key (`videos/<videoId|new>/covers/<sha256>.<ext>`). `Video.thumbnailUrl` / the `default_video_thumbnail` AppSetting store the object's S3-endpoint URL (`https://<account>.r2.cloudflarestorage.com/<bucket>/<key>`): absolute, but only readable with credentials. `ThumbnailResponseService` streams it via authenticated `GetObject`, never a plain `fetch`. This bucket must **never** get public access.
- **Public bucket** (`CLOUDFLARE_R2_BUCKET_THUMBNAILS_PUBLIC`, `polutek-thumbnails`, served at `NEXT_PUBLIC_R2_PUBLIC_HOST`) — holds **only** copies of `PUBLISHED` videos' thumbnails, same key, `Cache-Control: public, max-age=31536000, immutable`. `Video.thumbnailPublicUrl` records the copy.
- **Sync** (`lib/modules/video/application/sync-public-thumbnail.service.ts`): `publishAdminVideo` copies the thumbnail to the public bucket; `updateAdminVideo` (unpublish or new cover), `archiveAdminVideo` and `deleteAdminVideo` remove the public copy. When removing, the DB column is cleared **before** the object is deleted, so a failed delete never leaves a draft advertised. These hooks never throw — a failed sync just leaves the video on the proxy. `/api/cron/sync-public-thumbnails` (daily) re-syncs every video and deletes public objects no video advertises (older than 1h). Every status change that leaves `PUBLISHED` must go through one of those use cases or call `syncPublicThumbnail()` — do not add a new unpublish path without it.
- **Public DTOs** (`toPublicVideoDto`, `VideoContentService.mapToPublicVideoDTO`, the sidebar layout) use `resolvePublicThumbnailSrc()`: the public R2 URL only when the video is `PUBLISHED` **and** `thumbnailPublicUrl`'s key matches the current private key. Otherwise it's `/api/videos/[id]/thumbnail` (drafts, legacy Blob thumbnails, default fallback, a copy not synced yet). Never put a private storage URL in a public DTO.
- Uploads fall back to Vercel Blob while the R2 env vars aren't set; Blob URLs keep working through the proxy exactly as before. `scripts/migrate-thumbnails-to-r2.ts` (`npm run media:migrate-thumbnails-r2`, dry-run by default, `--apply`, `--rollback=<journal.json>`) copies existing Blob thumbnails to R2 and repoints the DB. It never deletes Blob objects: the previous values are kept in the journal until the R2 copies are verified.
- R2 credentials are shared with the video-originals bucket (`CLOUDFLARE_R2_ACCOUNT_ID` / `_ACCESS_KEY_ID` / `_SECRET_ACCESS_KEY`); the token needs Object Read & Write on both thumbnail buckets.

Still true:

- `/api/videos/[id]/thumbnail` streams the server-side source and enforces its own policy: published videos are public, drafts are admin-only.
- The route is listed as **public** in `middleware.ts` — do not remove it from `isPublicRoute`.
- Admin components render this proxy with `unoptimized` on `next/image` so draft thumbnails stay visible in the panel with admin cookies.
- `resolveVideoThumbnailUrl()` returns the raw storage/external URL for server-side streaming (a private R2 URL is absolute) — never a relative proxy path.
- Published-video thumbnails served by the proxy are CDN-cacheable (`PUBLIC_THUMBNAIL_CACHE_CONTROL`, includes `s-maxage`); draft thumbnails must always use `PRIVATE_THUMBNAIL_CACHE_CONTROL`.
- Default fallback chain is unchanged: `Creator.defaultThumbnailUrl` → `default_video_thumbnail` AppSetting (now an R2 or Blob URL) → `null`, always resolved via the proxy.

### 4.9 Comment Reactions

- `CommentReactionType` enum is `LIKE | DISLIKE` (one reaction per user per comment via `@@unique([userId, commentId])`).
- Dislike has **no public counter** — only `likesCount` is aggregated; deleting/replacing a DISLIKE must never touch `likesCount`.
- `toggleCommentLike` handles `LIKE`/`DISLIKE`/`UNLIKE` (clear). API: `PUT /api/comments/[id]/reaction` with optional body `{ type: "LIKE" | "DISLIKE" }`, `DELETE` clears any reaction.

### 4.10 Donation/Tip Widget — Always the Tip-Jar Variant (2026-09-22), One Payment Path

`app/components/channel/DonationBox.tsx` is the single tip widget rendered by `SidebarPlaylist.tsx`'s `PatronBox`.

**Update 2026-09-22 (collapsed to one variant — supersedes the "two copy variants" description below):** since `checkVideoAccess()` no longer gates PATRON-tier content on payment (§4.4), every signed-in viewer already has everything the old "non-patron" variant used to sell. `DonationBox` was changed to hardcode `const viewerIsPatron = true;` internally and ignore its incoming `viewerIsPatron` prop entirely (the prop stays in `DonationBoxProps`, marked `@deprecated`, purely so `SidebarPlaylist`/`SidebarSupportBox`'s existing `viewerIsPatron={viewerIsPatron}` call sites don't need to change). Concretely this means, for every signed-in viewer: `isTipGate` is always `true` ("Bramka Napiwkowa 💰" copy, never "Strefa Fenkjuu" pay-to-unlock copy), the amount field is always free-form starting empty (never a fixed non-editable gate price), and `minAmount` always resolves to `patronBoxMin` (never `patronThreshold`). The now-dead `!viewerIsPatron` branches (the old fixed-price effect, the old non-patron copy/bullets ternaries) were deliberately left in place rather than deleted, so this stays a small, easily-revertible diff if the business model ever reverts — do not "clean them up" without reintroducing the access gate at the same time, or the two will drift. `viewerIsPatron={viewerIsPatron}` is still passed down to `CheckoutModal`/`DonationAmountField` from `DonationBox`'s own (now-always-`true`) local value, so `CheckoutModal`'s dialog title also always reads "Send a one-time tip", never "Access to the Thank You Zone" — no code change was needed in `CheckoutModal.tsx` itself for this. `PolitykaContent`/`RegulaminContent` links inside the widget, the Terms/Privacy checkbox and the separate withdrawal-consent checkbox (§ below) are unchanged.

**Update 2026-09-22 (owner shortened the tip-gate copy):** the tip-gate `subtitle` and `bodyCopy` (a longer "Nic tu nie kupujesz..."/"Masz komplet..." paragraph) plus its three bullet points were replaced, at the owner's explicit request, with one line: PL `"Bez wsparcia widzów tego projektu by nie było. Dziękuję."`, EN `"This project wouldn't exist without viewers' support. Thank you."`. `subtitle` is now `""` for the tip-gate branch and its `<p>` is no longer rendered (an empty string there would've shown nothing anyway, but keeping a dead node was pointless); `bullets` is now `[]` for the tip-gate branch, and the `<ul>` only renders when `bullets.length > 0` so an empty list never leaves a stray empty element. The non-tip-gate branch (dead since the PATRON access-gate removal earlier the same day, §4.4) still has its own subtitle/bullets, untouched. `tests/unit/components/channel/DonationBox.test.tsx` still asserts the old long-form copy — add it to the same-day test-debt list above.

The description immediately below (two copy variants keyed on `viewerIsPatron`) is now historical — kept for context on how the component got here, not current behavior:

- **Non-patron viewer** (`viewerIsPatron` false/undefined): copy promised that a successful tip grants lifetime Thank You Zone access. *(No longer reachable — see the 2026-09-22 update above.)*
- **Existing patron** (`viewerIsPatron` true): the "Bramka Napiwkowa" / tip-jar variant — copy states access is already secured and this tip unlocks nothing new. *(Now the only variant, for every signed-in viewer.)*
- `PatronBox` only renders for **signed-in** users. The render gate uses Clerk's live client auth (`useAuth().isSignedIn`) in `SidebarPlaylist.tsx`, not the server-threaded `userProfile` prop. (Unchanged.)

**Update 2026-08-01:** a prior revision of this section described a separate `TipJarModal.tsx` (steps: disclaimer → recipient picker → channel picker → BLIK/crypto/Stripe) and a patron variant with no amount field/terms checkbox. That multi-channel tip modal was built and then fully reverted (see the "Revert the multi-channel tip modal" commit) — `TipJarModal.tsx` does not exist in the codebase. Both variants now share one card and one button (`onSupport()`, no arguments — reads `amount`/`isTermsAccepted` from component state), both showing the same `DonationAmountField` and terms checkbox regardless of `viewerIsPatron`; only the copy/title/bullets differ. **Do not re-add BLIK/crypto payment channels or a separate tip-jar modal without an explicit request** — if one comes, keep the same rule that mattered before: the Stripe branch must stay on the canonical path (`onSupport()` → `/api/checkout/create-intent` → `CheckoutModal` → return-URL reconciliation → `fulfillPayment()`), and any unverifiable display-only channel (BLIK/crypto) is safe only because it's patron-only and grants nothing.

**Navbar/Hero deep-link into DonationBox.** `Hero.tsx`'s below-player action row has a `supportAction` button (neutral gray, matching Share/like-dislike) that, on click, both dispatches the `polutek:open-support` window event (see the mobile tab handoff note below) and merges `?support=1` into the **current** path/query via `router.replace(`${pathname}?...#donations`, { scroll: false })` — never a duplicated payment flow, and never a bare `${getLocalizedHref(language, "home")}` push. `#donations` is `DonationBox`'s own anchor ID (also used by `AccessLockOverlay`/`CommentComposer`'s own `href="#donations"` links); a `useEffect` in `DonationBox.tsx` detects `?support=1` on mount, then strips the param via `router.replace()` so a refresh doesn't retrigger it. Because `PatronBox` only mounts for signed-in viewers, the button checks auth first and opens the sign-in modal for logged-out clicks instead of navigating somewhere with nothing to trigger.

**Update 2026-08-02:** that `?support=1` effect only calls the box's own `onSupport()` when it would actually proceed to checkout (`userId` present, `isTermsAccepted` already true, valid `amount`) — not unconditionally. `isTermsAccepted` always starts `false` on a fresh mount (no persistence across visits), so calling `onSupport()` unconditionally from the deep-link click made the terms checkbox flag its "accept the terms" error immediately on essentially every Wspieraj click, before the viewer had even seen the box — a real, consistently-reproducible bug, not an edge case. The error must only appear from a real, explicit click on the box's own "Wyślij napiwek"/pay button. The guarded auto-call still fast-paths a repeat tip when everything is already filled in (rare, but harmless); otherwise the deep link is now a no-op beyond revealing/scrolling to the box. Covered by `tests/unit/components/channel/DonationBox-deep-link-terms.test.tsx`, confirmed to fail against the old unconditional `onSupport()` call before this guard landed.

**Update 2026-08-01 (mobile tab handoff):** on mobile/tablet (`ChannelHome.tsx`, below `lg:`), the donation box only exists inside the "videos" tab (`SidebarPlaylist`), not the "comments" tab — so a click on Hero's Wspieraj button while on "comments" must also switch tabs. `ChannelHomeContent` has a `revealDonations()` callback (`useCallback`) that both the pre-existing `polutek:open-support` window event listener (shared by `AccessLockOverlay`'s CTA and Hero's Wspieraj button) calls: `setActiveTab("videos")` on mobile, then a `scrollIntoView`. Hero's button used to only navigate via `?support=1` and rely on a `useSearchParams()`-driven effect in `ChannelHome` to trigger the tab switch — that round-tripped through router/searchParams propagation and proved unreliable from the "comments" tab (the switch and/or scroll intermittently didn't happen). Dispatching the window event directly on click makes the tab-switch+scroll synchronous and independent of any router timing, exactly like the already-proven `AccessLockOverlay` path; the `?support=1` query param is now purely a secondary signal so `DonationBox` can call its own `onSupport()` once it mounts — it is no longer what triggers the tab switch. Before that, Hero's button used to `router.push` a bare `${getLocalizedHref(language, "home")}` URL, which dropped the currently-open video's `?v=` param and forced `ChannelHomeContent`'s route-change reset (back to the "comments" tab) — a full, jarring content swap that looked like a sudden jump rather than a tab change; merging into the current path/query instead keeps `?v=` intact, so only `activeTab` changes.

**Update 2026-08-02 (always-mounted mobile "videos" panel — simplified, replaces two prior overengineered attempts):** two earlier iterations tried to paper over a race where the mobile "videos" tab panel (and the `SidebarPlaylist`/`DonationBox` inside it) was only *conditionally rendered* (`{activeTab === "videos" && (...)}`) — first with a fixed-delay `scrollIntoView` to wait out its slide-in-from-top mount animation, then with a 150ms-interval/~3s-deadline poll to also wait out `SidebarPlaylist`'s own Clerk `authLoaded` check. Both were real races in principle but the added complexity didn't reliably fix the reported behavior in practice and made the code much harder to reason about. The actual fix: the mobile "videos" tab panel is now **always mounted**, hidden via a plain CSS `hidden` class toggle when inactive — exactly the same technique the "comments" panel already used (`activeTab === "videos" && "hidden"`), just applied symmetrically to both panels instead of only one. This means `#donations` exists in the DOM continuously once the viewer is signed in, regardless of which tab is currently showing, so there is nothing to wait for: switching tabs is just a class toggle, and `revealDonations()` is back to a single `setActiveTab("videos")` + one `scrollIntoView` call (no delay, no polling, no slide-in animation on the panel). The wrapper carries `data-testid="mobile-videos-panel"` so tests can assert on the tab's hidden/visible state directly instead of on the DOM node's presence. If a future change reintroduces conditional mounting or a mount animation for this panel, it must re-solve the "target may not exist yet" problem — don't drop the always-mounted approach without a plan for that.

**Update 2026-09-14 (one `/api/channel/sidebar` request for both mounts):** the consequence of the always-mounted panel is that `ChannelHome` renders **two** `SidebarPlaylist` instances on every page load (mobile panel + desktop aside), and each used to run its own `useEffect` fetch of `/api/channel/sidebar` — a `force-dynamic` route doing real DB work (`ChannelLayoutService.getSidebarLayout`: a `user.findUnique`, `MainChannelService.getRequired`, a `video.findMany` with `include: { creator: true }`) — so every anonymous page view did that work twice on the app's highest-traffic surface. `app/components/channel/sidebar-layout-request.ts` now owns that request: a module-level map of **in-flight** requests keyed by viewer identity (`sidebarLayoutViewerKey(userId, isSignedIn)`), which the second mount joins instead of firing its own. Both mounts stay mounted and both still render their own loading skeleton / error fallback — only the network call is shared. **That map is deliberately not a result cache**: the entry is deleted the moment the request settles, so a later mount or any auth-state change (which changes the key, and is already in the effect's `[authLoaded, authUserId, isSignedIn]` deps) always refetches and can never be served a stale, wrong-viewer layout — do not "optimize" it into a persistent cache, patron lock state is viewer-specific. Abort is preserved via subscriber refcounting: the shared `AbortController` fires only when the *last* subscriber detaches, so one instance re-running its effect can't cancel the other's in-flight request. Effects don't run during SSR, so the module-level map is client-only and never shared across requests. Covered by `tests/unit/components/channel/SidebarPlaylist-shared-fetch.test.tsx` (confirmed to fail with 2 fetches against the pre-fix component).

**Update 2026-08-02 (desktop regression — duplicate `#donations` id):** the always-mounted mobile panel above introduced a second bug: at desktop widths, **two** elements share `id="donations"` at once — the mobile "videos" panel's own `DonationBox` (still in the DOM, just CSS-hidden via `lg:hidden`) and the desktop aside's separate `SidebarSupportBox`/`DonationBox`. `document.getElementById` always returns the first match in DOM order — the hidden mobile copy — so on desktop the Wspieraj button's scroll silently targeted a `display:none` element and did nothing (worked fine on mobile, where that copy is the visible one). `getVisibleDonationsElement()` (`ChannelHome.tsx`, module-level) fixes this by querying all `[id="donations"]` matches and picking the one whose `offsetParent` isn't null (browsers null this out for `display:none` elements and their descendants). `revealDonations()` calls this instead of `document.getElementById`. Covered by `tests/unit/components/channel-home-support-handoff.test.tsx`, which mocks both `SidebarPlaylist` and `SidebarSupportBox` to each render their own `#donations` copy and stubs `offsetParent` per test (jsdom does no real layout, so this can't be verified any other way) to assert the scroll lands on whichever copy is actually visible for that viewport — this test was confirmed to fail against the old `document.getElementById` call before the fix landed. If either component's rendering ever changes such that only one `#donations` can exist at a time, this helper becomes unnecessary but remains harmless.

**Update 2026-09-20 (separate withdrawal-right waiver checkbox — art. 38(1)(13) Polish Consumer Rights Act):** every checkout surface (`DonationBox`, `SecretPledgeBox`, `SecretPledgeBox2`) now requires a **second, distinct** checkbox alongside the existing Terms/Privacy checkbox before `submit()` will proceed. The Regulamin/Terms body (§6, `LegalDocs.tsx`) already stated that payment requires "wyraźną zgodę na rozpoczęcie dostarczania treści cyfrowych przed upływem terminu do odstąpienia od umowy oraz ... potwierdzenie ... utraty prawa odstąpienia" — but that was only prose inside a linked legal document, never a separate affirmative action at the point of purchase. A single bundled "I accept the Terms" checkbox does not satisfy art. 38(1)(13): the consumer must explicitly confirm *this specific* consent (immediate digital-content delivery before the 14-day withdrawal period expires, plus acknowledgment of losing that right) before paying, or the waiver is not legally valid and the consumer retains the statutory withdrawal right even after receiving lifetime Patron access.

Implementation: `useCheckoutFlow()` (`lib/hooks/useCheckoutFlow.ts`) owns `isWithdrawalAcknowledged`/`showWithdrawalError`/`onWithdrawalCheckedChange`, gated in `submit()` immediately after the existing terms check (same fail-closed pattern: unchecked → block and show the error, never silently proceed). `t.acceptWithdrawal`/`t.pleaseAcceptWithdrawal` (`LanguageContext.tsx`) hold the one shared PL/EN wording for the checkbox label and its validation error, reused by all three surfaces so the legally significant text has a single source of truth instead of drifting across three components. `DonationBox.tsx`'s `?support=1` deep-link auto-submit effect was updated to also require `isWithdrawalAcknowledged` (mirroring its existing `isTermsAccepted` guard) so it can't silently bypass this consent. Applied uniformly to **both** DonationBox variants (the non-patron access gate and the existing-patron "Bramka Napiwkowa" tip jar) rather than only the access-granting path — simpler to reason about with one shared gate, and asking for consent that turns out to be unnecessary for a pure additional tip carries no legal risk, whereas omitting it somewhere it was needed would. Covered by `tests/unit/hooks/useCheckoutFlow.test.tsx` (hook-level gating) and `tests/unit/components/channel/DonationBox.test.tsx` (component-level: proves the Terms checkbox alone is insufficient and the withdrawal error is distinct), both confirmed to fail against the pre-fix code. Do not merge this back into the Terms checkbox or make it optional without confirming with the owner/legal first.

**Flag (2026-09-22, resolved same day — see the entry immediately below):** the new Regulamin/Terms text (see the "major" 2026-09-22 entry near the top of this file) reframes support as a non-refundable gift that is explicitly **not** a sale of digital content and grants no access — the exact premise art. 38(1)(13)'s withdrawal-right waiver applies to. This checkbox was deliberately left in place rather than removed, because removing a legally-motivated consent step is a bigger call than adding one and wasn't asked for — but there is now a real open question (like the Polityka/Regulamin entity mismatch flagged elsewhere in this file) of whether it's still legally required for a pure donation.

**Update 2026-09-22 (withdrawal checkbox removed — the question above is resolved):** the owner asked directly whether this checkbox is still needed, and specifically to check a Polish Supreme Administrative Court (NSA) ruling on streamer "donejty" from roughly two months earlier: **NSA, 7 July 2026, case III FSK 113/26.** That ruling (a tax case, on whether anonymous streamer donations count as tax-exempt `darowizna`/gift rather than taxable income) sets the test for gift status: known amount, paid from the donor's own assets, **completely gratuitous, with no reciprocal benefit whatsoever** — the court and commentary are explicit that *any* benefit granted in exchange (their own example: access to extra content) disqualifies the payment from gift treatment. Separately, Polish consumer-protection law (Ustawa o prawach konsumenta, the withdrawal-right chapter arts. 27–38, including the art. 38(1)(13) exception this checkbox implements) only applies to contracts involving a price/reciprocal consideration from the trader — a genuine no-consideration `darowizna` falls outside that chapter's scope entirely, so there is no withdrawal right to waive in the first place.

Put together: (1) the 2026-09-22 access-gate removal (§4.4/§4.10 above) already made support grant literally nothing, which is exactly what this ruling requires for gift/tax-exempt treatment — so that change was legally load-bearing, not just a UX one; and (2) once there is truly no reciprocal benefit, this checkbox's own legal basis (a sale-of-digital-content contract to waive withdrawal from) no longer exists — keeping consumer-sale language like "prawo odstąpienia od umowy" around a transaction that must read as a pure gift actively cuts against the "nic nie otrzymuje w zamian" test the ruling turns on. The checkbox, its state (`isWithdrawalAcknowledged`/`showWithdrawalError`/`onWithdrawalCheckedChange` in `useCheckoutFlow.ts`), its `submit()` gate, and its `t.acceptWithdrawal`/`t.pleaseAcceptWithdrawal` translation strings were removed from all three checkout surfaces (`DonationBox`, `SecretPledgeBox`, `SecretPledgeBox2`) and from `useCheckoutFlow.ts`/`LanguageContext.tsx`. The ordinary Terms/Privacy checkbox is unchanged and still required. **This is not a substitute for the owner/a lawyer independently verifying the ruling and its application** — it's an AI agent's reading of publicly available legal commentary, done because the owner explicitly delegated the decision in-thread; flag it for a real legal review if the donation model is ever scrutinized. `tests/unit/hooks/useCheckoutFlow.test.tsx` and `tests/unit/components/channel/DonationBox.test.tsx` still assert the removed checkbox's behavior and need updating (same test-debt category as the access-gate change above — no test runner in this environment to verify against).

**Consequence for `/secretproject`/`/secretproject2` noticed while making this change:** `SecretPledgeBox`'s own code comments still describe "the fixed gate price... unlocks the reward exactly as campaign copy promises" — but since the PATRON tier is no longer payment-gated at all (§4.4), the campaign's "locked reward video" has *already* been unlocked for any signed-in viewer since the same-day access-gate change, independent of any pledge. That comment/copy is now stale, in the same category as the DonationBox comments flagged stale in §4.10 above. Whether the campaign pages' pledge-tier copy itself should be rewritten to match (it currently still reads as "pledge to unlock") was not in scope of this pass — flag for the owner if `/secretproject`/`/secretproject2` copy is revisited.

Three distinct per-currency minimums must not be conflated:

- **Checkout floor** — `minAmountMinor` / `getPaymentCurrencyLimits()` / `MIN_PAYMENT_BY_CURRENCY`.
- **Patron threshold** — `patronThresholdMinor`, resolved by `resolvePatronThresholdMinor()`.
- **Patron-box minimum** — `patronBoxMinMinor`, the smallest free-form amount an existing patron may tip.

`GET /api/payment-settings` returns all three (`limits`, `patronThresholds`, `patronBoxMinimums`). Stripe Elements render in the viewer's language; keep `DonationBox.tsx` and `CheckoutModal.tsx` copy synchronized.

### 4.11 Language Resolution (pl/en)

- Initial UI language is resolved server-side in `lib/i18n/server-language.ts` (`resolveInitialLanguage()`), called from `app/layout.tsx` and threaded through `Providers` → `LanguageProvider`.
- Priority: signed-in user's DB `User.language` → `app-language` cookie → Vercel geolocation header → `Accept-Language` → `en`.
- `LanguageContext.tsx` mirrors every change to both `localStorage` and a one-year `app-language` cookie.
- Logged-in changes additionally persist to DB via `PATCH /api/user/language` and to Clerk metadata. Transactional/broadcast emails send in stored `User.language`.
- **Update 2026-09-13:** both language write paths — the `PATCH /api/user/language` route and the `updateUserLanguage` server action in `lib/actions/user.ts` (used by `ClerkLocalizationProvider.tsx`) — now go through the single `updateUserLanguage` use case (`lib/modules/users/application/update-user-language.use-case.ts`) with a `ClerkIdentityProvider`. The parallel `UserLanguageService` that the server action used to call was deleted; it bypassed `UserRepository`/domain errors and let a soft-deleted user write a language, which the route rejected. That use case also no longer passes an explicit `null` for `name`/`username`/`imageUrl` when the local row already exists — it did, which meant every language change blanked the stored display profile. Keep those fields `undefined` unless they were actually resolved from the identity provider.

### 4.12 Auth UI

- Clerk remains the backend and session authority.
- Public auth/account UI is custom/headless: `AuthModalProvider`, `AuthModal`, `UserMenu`, `AccountModal`, and related OAuth helpers.
- Do not replace the current custom UI with default Clerk widgets unless owner explicitly asks for a rollback.
- Do not implement a custom auth backend; use Clerk headless APIs/hooks.

### 4.13 Public App Shell, Preload and Install UX

- The root public experience should render app shell immediately; no blocking splash/ENTER gate (nothing the viewer has to click through).
- **Update 2026-09-26 (coordinated first reveal — fixes the "popcorn"/twinkling load):** the home page used to pop in piece by piece (navbar auth skeleton, sidebar skeleton → client fetch, player loader card, comments skeleton), each on its own schedule. Now:
  - `PageRevealGate` (`app/components/preload/PageRevealGate.tsx`, wrapped around the page in `HomeExperience.tsx`) keeps one calm full-screen `SitePreloaderScreen` (glasses logo + thin bar) on top of the already-rendered page until Clerk `isLoaded`, the featured poster is decoded, and the `player`/`comments` pieces report settled via `usePageRevealReady(key, ready)` (`revealKey="player"` from `Hero`, and `EmbeddedComments`), then fades the whole page in at once. `player` means the stream is actually playable, not just that access was granted: when the player mounts, `PremiumWrapper` stays silent and `VideoPlayer` reports on Vidstack's `canplay` (or on an error/timeout state); locks, coming-soon and error overlays are reported by `PremiumWrapper` itself. While the preloader covers the page, `VideoPlayer` buffers with `preload="auto"` but holds autoplay back (`usePageRevealCovering()`), then calls `play()` the moment it lifts, so the video starts from its first frame when the viewer sees it. It is **not** a click gate: it always leaves after `PAGE_REVEAL_MAX_WAIT_MS` (3s from navigation start, at least 0.7s after a late hydration), CSS hides it after 6s even without JS, and later mounts in the same document skip it. Don't add new `waitFor` keys for below-the-fold or slow things — the max wait would just hide them behind the preloader.
  - `app/[locale]/(home)/loading.tsx` renders the same `SitePreloaderScreen` (it used to render `HomePageSkeleton`), so "server streaming" and "client settling" read as one continuous screen. `HomePageSkeleton` is currently unused.
  - The sidebar layout is built server-side in `HomeExperience` (`ChannelLayoutService.getSidebarLayout`, JSON round-tripped to the `/api/channel/sidebar` shape) and passed down as `initialLayout`/`initialLayoutViewerKey`; `SidebarPlaylist` skips its first fetch when the client viewer key matches, and still refetches on any auth change or if the server load failed (null). This also removes one `/api/channel/sidebar` call per page view.
  - In-session video switches are poster-first: `PremiumWrapper` (via Hero's `posterUrl`) and `VideoPlayer`'s pre-playback overlay both render `PlayerPosterLoading` — the video's own `thumbnailUrl` at once, with a spinner that only fades in after ~450ms — instead of the old "Już podaję film…" loader card; the overlay fades out on the first real frame. Hover/focus/touch intent and the idle warm-up of nearby videos now also prefetch the first page of comments (`includeComments: true`), so a switch usually lands on ready comments.
- `AppPreloadProvider` is the current in-memory preload layer for playback plans, posters and comments.
- `ServiceWorkerCleanup` disables persistent service-worker interception/caches. Do not add offline/persistent caching for auth, payments, media-source, playback, or user-specific data without a focused design.
- `InstallAppMenu` handles add-to-home-screen/install affordances, including iOS instructions.

### 4.14 What NOT To Do

- Do not recreate retired control-plane docs (`docs/reports`, `docs/roadmap`, `docs/templates`, or governance operating-mode files); durable guidance belongs in this file, `KNOWN_LIMITATIONS.md`, or `docs/audit/`.
- Do not reintroduce legacy patron cache fields on `User` or treat Clerk metadata as patron authority.
- Do not bypass `fulfillPayment()` for Stripe success, replay, or reconciliation flows.
- Do not expose playable video URLs/tokens/provider IDs, mount players, or log views for denied playback plans.
- Do not replace the custom/headless Clerk UI with default widgets or a custom auth backend unless explicitly asked.
- Do not add persistent/offline caches for auth, payments, media-source, playback, or user-specific data without a focused cache-safety design.
- Do not add a second get-or-create-user path. `getOrCreateCurrentUser()` (→ `sync-user.use-case`) is the only one: it runs in a transaction that renames a stale row holding the same email and repoints that row's comments, payments and patron grants onto the surviving Clerk id. A plain `user.upsert`/`create` with the Clerk email instead hits the `User.email` unique constraint and leaves the history orphaned. The naive `GetOrCreateUserUseCase` that `/api/user/sync` and `/api/subscriptions` used was deleted on 2026-09-13 for exactly this.

---

## 5. Admin Panel

Located in `app/admin/`. Key areas:

| Path | Purpose |
|---|---|
| `/admin` | Dashboard overview |
| `/admin/videos` | Video list with filters (`VideoTable`, `VideoFilters`) |
| `/admin/videos/[id]` | Video detail (media, diagnostics) |
| `/admin/videos/[id]/edit` | Metadata edit form |
| `/admin/videos/new` | Create video (Cloudflare upload or existing UID) |
| `/admin/users` | User list |
| `/admin/users/[id]` | User detail + patron diagnostics |
| `/admin/comments` | Comment moderation + reports link |
| `/admin/comments/reports` | Reported comments queue |
| `/admin/emails` | Email broadcast |
| `/admin/channel` | Channel settings (name, bio, banner, default thumbnail URL) |
| `/admin/settings` | Media settings (default thumbnail file upload via Vercel Blob) |

`AdminLayoutShell` (`app/admin/videos/components/AdminLayoutShell.tsx`) does **not** wrap all admin pages — it is only used by the videos surfaces: `app/admin/videos/page.tsx`, `app/admin/videos/[id]/edit/page.tsx`, and `AdminVideoEditView.tsx`/`AdminVideoErrorView.tsx`. Every other admin page (users, comments, comment reports, channel, settings, emails, the videos `[id]` detail page) hand-rolls its own `Navbar` + container wrapper, with inconsistent background/padding treatments — there is no single shared page-shell component across the admin panel today. See `docs/tickets/ready/ADMIN-LIST-SCAFFOLD-CONSOLIDATION-001.md` for the tracked consolidation work. `AdminNavigation` (a separate, actually-shared component) provides breadcrumb back-navigation and is used consistently across admin pages.

Default thumbnail fallback priority:

1. `Creator.defaultThumbnailUrl` — URL field set via `/admin/channel`.
2. `AppSetting` key `default_video_thumbnail` — Vercel Blob URL set via `/admin/settings` file upload.
3. `null` — no fallback.

**Update 2026-08-02:** `updateAdminVideo()` (`lib/modules/video/application/update-admin-video.use-case.ts`) only re-validates Hero eligibility (`VideoPolicy.getHeroBlockers`, which includes the "must have an active playback route in READY state" check) when the update actually **promotes** a video to Hero (`input.isMainFeatured` true while `existing.isMainFeatured` was false/unset) — not on every save. The `/admin/videos/[id]/edit` form always resubmits the video's current `isMainFeatured` value regardless of what the admin actually changed, so re-validating on every save meant a video that became Hero before the active-playback-route invariant existed (or otherwise has a legacy gap there, e.g. a working `isPrimary` asset with no backfilled `VideoPlaybackRoute` row) could never be edited again — even a trivial title change would fail with `VIDEO_INVALID_HERO: VIDEO_PUBLICATION_MISSING_ACTIVE_ROUTE`, despite the video playing fine on the live site. Covered by a regression test in `tests/unit/modules/video/update-admin-video.use-case.test.ts`, confirmed to fail against the old unconditional check before the guard landed.

---

## 6. Cron Jobs

Registered in `vercel.json` under `crons`. All cron routes live in `app/api/cron/`. Auth via `Authorization: Bearer <CRON_SECRET>` header.

**Uwaga:** Cron `stripe-reconciliation` został usunięty z `vercel.json`, ponieważ konto Hobby na Vercel nie obsługuje harmonogramów częstszych niż raz dziennie. Trasa API `/api/cron/stripe-reconciliation` nadal istnieje i działa — przy upgrade na plan Pro wystarczy dodać:

```json
{ "path": "/api/cron/stripe-reconciliation", "schedule": "*/15 * * * *" }
```

Co robi ten cron: co 15 minut szuka płatności `PENDING` starszych niż 15 min (do 7 dni) i odpala `fulfillPayment()` ponownie — zabezpieczenie na wypadek utraty webhooka Stripe. Bez crona patron może czekać na dostęp do czasu ręcznej interwencji lub ponownej próby webhooka przez Stripe.

| Route | Schedule (Pro) | Purpose |
|---|---|---|
| `/api/cron/stripe-reconciliation` | `*/15 * * * *` | Recovers stuck `PENDING` payments by re-running `fulfillPayment()` or marking as failed |
| `/api/cron/video-provider-jobs/reconcile` | `0 4 * * *` (registered; daily works on Hobby) | Polls provider status for stuck import jobs (missed webhooks), restarts imports that never reached the provider, fails them with a clear reason after max attempts |
| `/api/cron/prune-playback-sessions` | `0 5 * * *` (registered; daily works on Hobby) | Deletes `VideoPlaybackSession` rows older than 30 days that were never counted as a view (`pruneStalePlaybackSessions()`, `lib/modules/video/`) |
| `/api/cron/sync-public-thumbnails` | `0 6 * * *` (registered; daily works on Hobby) | Safety net for R2 thumbnail sync: makes the public thumbnail bucket hold exactly the published videos' thumbnails (`reconcilePublicThumbnails()`, see §4.8). No-op until the public bucket/host env vars are set |

The daily cron is only the safety net for video provider jobs. The primary recovery path is on-demand: `POST /api/admin/videos/[id]/reconcile` now runs the provider-job reconciler scoped to that video before route policy, and the admin media panel calls it from the "Odśwież" button plus an automatic 15s poll while the pipeline is in `CREATING_SOURCES`/`PARTIALLY_READY`. Do not revert the media panel to a passive DB-state read — without provider polling, a missed webhook leaves targets in "Tworzę źródło" forever.

**`prune-playback-sessions` (added 2026-09-20):** `AppPreloadProvider`'s hover/idle prefetch creates a real `VideoPlaybackSession` row via the same `PlaybackService.createPlaybackPlanWithContext` path as actual playback, for every preloaded video regardless of whether the viewer ever plays it — so the table grows with page traffic, not just views. This cron deletes rows older than `STALE_PLAYBACK_SESSION_RETENTION_DAYS` (30) where `countedAsView` is still `false`; it never touches a session that was ever counted as a real view, and never touches `countedAsView`/aggregated view-count logic elsewhere. Confirmed before adding this: no admin diagnostics, patron read model, or Stripe dispute logic reads `VideoPlaybackSession` — it's pure telemetry with no downstream consumer that needs long retention. `VideoPlaybackEvent.session` is `onDelete: SetNull`, so deleting a session never cascades into deleting its events; they just lose the session link.

---

## 7. Key Files to Know

| File | Role |
|---|---|
| `app/page.tsx` | Root `/` route; resolves locale and redirects to `/[locale]` |
| `app/[locale]/(home)/page.tsx` | Public home/channel entry; dynamic page with cached/loaded public content and per-user state. Route-grouped with its `loading.tsx` so the full `HomePageSkeleton` doesn't leak onto sibling `app/[locale]/*` routes |
| `app/components/ChannelHome.tsx` | Public app shell composition, selected video state, local player transition |
| `app/components/preload/AppPreloadProvider.tsx` | In-memory preload/warm layer for playback plans/posters/comments |
| `app/components/VideoPlayer.tsx` | Vidstack player, text tracks, telemetry, custom controls |
| `app/components/PremiumWrapper.tsx` | Access/playback plan fetch and locked/loading states |
| `app/components/auth/AuthModalProvider.tsx` | Custom/headless Clerk modal provider |
| `app/components/auth/AuthModal.tsx` | Custom sign-in/sign-up/password reset UI over Clerk headless APIs |
| `app/components/auth/UserMenu.tsx` / `AccountModal.tsx` | Custom account UI over Clerk user APIs |
| `app/components/channel/SidebarPlaylist.tsx` | Sidebar sections, lock badges, support box signed-in gate |
| `app/components/channel/DonationBox.tsx` | Support/patron tip widget and Stripe checkout entry |
| `app/components/InstallAppMenu.tsx` | Install/add-to-home-screen affordance |
| `lib/modules/patron/application/grant-patron.use-case.ts` | Grant patron status |
| `lib/modules/patron/application/revoke-patron.use-case.ts` | Revoke patron status |
| `lib/modules/patron/application/recalculate-patron-status.use-case.ts` | Pure read — derive status from active grants |
| `lib/modules/payments/application/fulfill-payment.use-case.ts` | Canonical, replay-safe payment fulfillment |
| `lib/modules/access/application/check-video-access.use-case.ts` | Gatekeeper for video access |
| `lib/modules/playback/application/playback.service.ts` | Resolves playable video source based on access plan |
| `lib/modules/playback/domain/playback-policy.ts` | Policy gates for legacy private playback fallback (always false) |
| `app/api/media/[...path]/route.ts` | Media proxy using playback policy |
| `app/api/webhooks/stripe/route.ts` | Stripe webhook handler |
| `app/api/webhooks/cloudflare/route.ts` | Cloudflare Stream webhook handler |
| `lib/modules/users/application/patron-read-model.ts` | Patron diagnostics read model |
| `lib/modules/media/application/default-thumbnail.service.ts` | Resolves fallback thumbnail URL |
| `lib/modules/media/infrastructure/thumbnail-response.service.ts` | Streams thumbnails with correct cache policy |
| `lib/services/payment.service.ts` | Deprecated bridge used only in tests; production uses modular use cases |
| `prisma/schema.prisma` | Database schema (single-writer) |

---

## 8. Environment Variables

See `.env.example` for all variables. Critical ones:

| Variable | Purpose |
|---|---|
| `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` / `CLERK_SECRET_KEY` | Auth |
| `DATABASE_URL` / `DATABASE_URL_UNPOOLED` | Neon PostgreSQL |
| `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` | Stripe |
| `CLOUDFLARE_ACCOUNT_ID` / `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_WEBHOOK_SECRET` | Cloudflare Stream |
| `RESEND_API_KEY` / `EMAIL_FROM` | Email |
| `BLOB_READ_WRITE_TOKEN` | Vercel Blob legacy media/thumbnail support |
| `CRON_SECRET` | Authenticates cron API routes (≥32 random chars) |
| `ADMIN_CLERK_USER_IDS` | Comma-separated Clerk user IDs with admin access |
| `UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN` or `KV_REST_API_URL` / `KV_REST_API_TOKEN` | Production rate limiting |

---

## 9. Documentation and Issue Hygiene

- `docs/tickets/ready/` is the only executable file-based ticket queue.
- Legal/operator/evidence scope belongs to issue #1269 unless split into a small implementation ticket.
- Closed/history work belongs in git, PRs and issues, not in living roadmap files.
- If a PR changes behavior that agents must preserve, update this file in the same PR.
- Do not overwrite historical PR bodies unless explicitly asked; update open issues or living docs instead.
