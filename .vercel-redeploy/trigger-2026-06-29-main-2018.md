# Vercel redeploy trigger — latest main

Triggered on 2026-06-29 20:18 Europe/Warsaw to force a fresh production deploy from the latest `main` branch.

Expected changes included:

- experiment4 should no longer use the rough mock layout
- experiment4 should use the canonical Navbar → ChannelHome → Footer shell
- CSP should allow browser PUT uploads to Cloudflare R2 presigned URLs
- Next image optimizer should read configured thumbnail hosts from env
